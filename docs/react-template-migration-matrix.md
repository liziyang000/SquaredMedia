# React 模板迁移矩阵

本文逐一覆盖 `template/pingfangvideo/html/**` 下当前 79 个 MacCMS 模板或片段，固定它们在 React 前台迁移后的归属、路由、数据契约和验收边界。实施顺序与总体约束以
[`docs/superpowers/plans/2026-07-21-react-frontend-migration.md`](superpowers/plans/2026-07-21-react-frontend-migration.md)
为准。

## 固定规则

- React 使用不包含 `index.php` 的新页面 URL；旧的公开 GET 页面地址必须一对一、单跳 `301` 到对应新 URL。
- 表中的旧 URL 以 MacCMS action 为事实源，并给出默认动态地址示例。生产站可能启用了后台自定义 rewrite，切流前必须以实际 `mac_url*` 输出和访问日志补齐所有别名。
- POST、Ajax、验证码、播放器和下载授权端点不是页面地址，不能 `301` 到 React。它们继续由 MacCMS 或插件处理，React 通过同源 API 调用。
- 新会员注册和账号找回不进入 React 前台范围；新地址 `/register`、`/forgot-password` 以及旧地址 `/index.php/user/reg.html`、`/index.php/user/findpass.html` 的 GET/HEAD 请求均按 `410 Gone` 退场，不重定向到登录页或首页。
- 不实施 SEO。百度、Google Sitemap 模板明确返回 `410 Gone`，不能进入 SPA 回退。
- RSS 不在 React 中重建。只有访问日志或外部系统能证明存在消费者时才由 MacCMS 原端点直通；否则按 `410` 退场。
- `vod/player.html` 是试看、收费和权限播放器的内部承载面，继续由 MacCMS 直通。React 只负责播放页外壳，不得从普通列表、详情或缓存接口提前取得原始播放地址。
- 当前仅输出“维护中”“暂未启用”等文案、没有可用业务列表或操作的兜底模块按 `410` 退场。生产切流前仍需核对后台模块开关、数据量和近 30 天访问日志；若发现真实业务使用，必须重新进入 React/API 迁移范围，不能直接 `410`。

## 事实来源

- 当前职责、字段和交互以仓库内 79 个 `template/pingfangvideo/html/**` 文件及 `template/pingfangvideo/js/app.js` 为准。
- 总体架构、干净 URL、无 SEO 和播放器边界来自本仓库 React 迁移实施计划。
- MacCMS 模板文件职责以[官方模板结构](https://www.maccms.la/theme/structure)为准；视频对象、播放参数及 `$player_data`、`$player_js` 语义以[官方视频模板文档](https://www.maccms.la/theme/theme-vod)为准；默认 rewrite 名称参考[官方默认路由](https://www.maccms.la/config)。

## 验收代码

矩阵使用下列验收代码减少重复描述；每一行中的附加条件同样是必验项。

| 代码 | 验收要求                                                                                                                                                               |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `R1` | React 新路由可站内进入、直接访问和刷新；成功、空数据、404、超时均有确定状态；旧 GET URL 保留参数并单跳 `301`；桌面与 320、390、1180、1181 像素无横向溢出和控制台错误。 |
| `R2` | 在 `R1` 基础上覆盖匿名、已登录、会话过期和设备撤销；写请求验证同源 Cookie、Origin 或 CSRF、输入、重复提交、验证码、成功和业务失败；用户响应不可共享缓存。              |
| `R3` | 在 `R2` 基础上用真实 HLS、MP4、第三方解析、试看或付费线路验证 `sid`、`nid`、上下集、记录写入和自动下一集；直接请求或修改参数不能绕过授权，普通 API 不泄露播放地址。    |
| `C1` | React 共享组件有单元和交互测试，并在所有消费页面验证键盘、焦点、加载、空态与错误态；删除旧片段前搜索确认没有残余 include 或脚本钩子。                                  |
| `B1` | MacCMS 端点优先于 SPA 回退，保持正确状态码、内容类型、Cookie 和权限；React 构建产物不包含其敏感响应，匿名请求不可得到受限数据。                                        |
| `F1` | 先用日志或调用方清单确认 RSS 消费者；保留时验证 XML、`Content-Type`、更新时间和链接可达且端点不经 SPA；无消费者时改用 `X1`。                                           |
| `X1` | 所有已知旧 URL 与 rewrite 别名返回精确 `410 Gone`，不重定向、不输出 React 入口；切流前有后台数据量和近 30 天访问日志的零业务证据。                                     |

## 迁移统计

| 最终归属              | 数量 | 说明                                                    |
| --------------------- | ---: | ------------------------------------------------------- |
| React 页面            |   26 | 首页、视频、既有会员账号、设备、评论和反馈页面。        |
| React 共享组件        |   17 | 公共骨架、系统状态、列表片段、评分交互和用户 include。  |
| MacCMS 后端直通或候选 |    5 | 1 个播放器内部页面；4 个仅在有消费者时保留的 RSS 输出。 |
| `410` 退场            |   31 | 25 个无业务数据兜底、4 个 Sitemap 和 2 个账号流程页面。 |
| 合计                  |   79 | 与当前目录文件数一致。                                  |

## 当前实现状态

- 79 个模板或片段已经全部获得 React 页面、React 共享组件、MacCMS 后端直通候选或 `410` 退场归属；矩阵无遗漏。当前切流范围仅为 `react.ping2.my` staging，主站保持原状。
- Next.js Proxy 的代码和契约测试已覆盖已知旧公开 GET/HEAD 地址的单跳 `301`、参数保留，以及明确退场地址的真实 HTTP `410`。这些规则只用于迁移兼容，不实施 SEO；Sitemap、搜索引擎元数据和 SEO 切流均不在范围内。staging Nginx 仍让 `/index.php/**` 优先走 PHP，因此不声称旧 PHP 地址已经在服务器上执行这些重定向。
- App Router 的未匹配路由可进入框架级 `not-found`；但影片详情、下载、剧情等动态页面当前仍在客户端请求 API。API 404 只映射为 `MissingContentPage` UI，API 403 映射为现有权限 UI，尚未实现这些动态页面的真实服务端 HTTP 404/403 状态。
- 匿名历史保存在经过结构和 URL 白名单校验的浏览器存储中。账号收藏和播放历史通过私有 session 接口读取，DTO 用 `recordIds` 保留每条原生 `ulog_id`；选择删除只按当前用户、日志类型和精确 `ulog_id` 删除，不按 `ulog_rid` 扩大到同一影片的其他记录，清空操作仍限制在当前用户对应的 type 2 或 type 4 范围内。
- 影片库、分类、搜索和年度榜已统一使用服务端 scope、父子分类上下文、真实 facets 与 24 条分页；详情保留线路名称/提示和评分顶踩计数，下载与剧情分别读取原生 `vod_down_*` 和 `vod_plot_*`。下载 API 的 404/403 分别显示影片不存在/下载权限状态，剧情 API 的 404/403 分别显示影片不存在/详情权限状态；两者都是客户端 UI 分流。
- 账号前台只保留既有会员登录、会话、账号首页、收藏、播放记录、设备和退出登录；不提供新会员注册或账号找回。`/register`、`/forgot-password` 及两个旧 GET 页面地址的本地路由与 Nginx 模板均已配置为对 GET/HEAD 返回 `410 Gone`；本次未部署，不能据此声称 staging 已完成该状态码切换。
- 留言、片源报错和评论已读取 MacCMS 的 `enabled`、登录、验证码和 `audit` 配置；关闭时前端不展示提交面板或不发起评论列表请求，PHP API 仍独立拒绝写入。接口返回 `pending`/`published` 后，前端明确区分“等待审核”和“已发布”，不把通用成功消息当作已公开。
- production build 使用 Linux standalone、systemd 健康检查、版本目录、`current` 原子切换和 Nginx 反向代理，不携带 fixture、本地适配器或媒体样例地址。真实 MacCMS API 已保持同源直通；普通会员基础流程只有有限的 staging 验收证据，VIP、付费、试看、密码、版权等真实会员矩阵仍未完成验收，不能以代码或契约测试代替。

## 逐模板矩阵

### 首页

| 模板                                           | 原职责 / 旧路由                                     | 最终归属   | 新路由或承载组件 | 关键数据 / 交互                                                                                                                                                                                                                                                                                                 | 验收方式                                                                |
| ---------------------------------------------- | --------------------------------------------------- | ---------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `template/pingfangvideo/html/index/index.html` | 站点首页；MacCMS action `index/index`，公开地址 `/` | React 页面 | `/`，`HomePage`  | `$maccms.site_name`、当日更新数、固定视频分类 42、47、48、57、111；热播和本年榜单所需 `vod_id`、`vod_name`、`vod_pic`、`vod_pic_slide`、`vod_year`、`vod_class`、`vod_duration`、`vod_version`、`vod_content`、`vod_remarks`、`vod_score`、`vod_hits`；轮播、分类标签、本地及账号观看记录、主题、空态和图片回退 | `R1`；首页 API DTO 白名单对照；production build 不包含 fixture 或播放源 |

### 视频页面

| 模板                                              | 原职责 / 旧路由                                                                                 | 最终归属            | 新路由或承载组件                                                                        | 关键数据 / 交互                                                                                                                                                                                                                                       | 验收方式                                                                                                                    |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `template/pingfangvideo/html/vod/confirm.html`    | 点播或积分确认模板；由 `vod/confirm` 权限流程选择，默认动态地址随目标视频生成                   | React 页面          | `/vod/:vodId/confirm`，`AccessConfirmPage`                                              | 与旧模板一致：读取安全访问状态、积分或会员提示，仅提供返回详情/影片库入口，不在 React 中自行扣费或伪造购买                                                                                                                                            | `R2`；确认页不能绕过原生授权；真正购买仍由 MacCMS 原生流程负责                                                              |
| `template/pingfangvideo/html/vod/copyright.html`  | 版权或地区限制提示；由视频控制器选择 `vod/copyright`                                            | React 页面          | `/vod/:vodId/unavailable`，`AvailabilityPage`                                           | `vod_id`、`vod_name`、限制原因和可返回的详情地址；无写操作                                                                                                                                                                                            | `R1`；受限影片不得在播放桥接端点返回播放器                                                                                  |
| `template/pingfangvideo/html/vod/detail.html`     | 视频详情；action `vod/detail`，默认 `/index.php/vod/detail/id/:vodId.html`                      | React 页面          | `/vod/:vodId`，`VodDetailPage`                                                          | `vod_id`、`vod_name`、`vod_pic`、`vod_remarks`、`vod_score`、`vod_score_num`、`vod_up`、`vod_down`、类型、年份、地区、类别、简介、导演、演员、热度、更新时间；播放组仅返回 `sid`、来源、提示、集数 `nid` 和名称；相关推荐；评分、顶踩、收藏、浏览历史 | `R2`；收藏走用户日志 type 2；顶踩和评分契约测试；详情响应不得包含原始集地址                                                 |
| `template/pingfangvideo/html/vod/detail_pwd.html` | 视频详情密码验证；同一 `vod/detail` 请求被控制器切到密码模板                                    | React 页面          | `/vod/:vodId/unlock?scope=detail`，`PasswordChallengePage`                              | `vod_id`、`vod_name`；同源 CSRF POST 验证内容密码，写入与官方 `MAC.Pwd.Check` 的 `data-type=1` 相同的 session 后安全回跳                                                                                                                              | `R2`；错误密码、暴力尝试限制和详情/播放/下载三类 session 隔离                                                               |
| `template/pingfangvideo/html/vod/down.html`       | 视频下载页；action `vod/down`，默认 `/index.php/vod/down/id/:vodId.html`，选择项带 `sid`、`nid` | React 页面          | `/vod/:vodId/download`，`DownloadsPage`                                                 | `vod_id`、`vod_name`；用 `mac_play_list(...,'down')` 返回下载组、来源、提示和条目标识；点击同源 `vod/down` 路径让 MacCMS 再鉴权，列表接口不返回原始资源 URL；API 404 显示 `MissingContentPage`，403 进入 `download` 权限状态                          | `R2`；逐条验证下载权限、密码、积分、错误线路和地址不泄露；当前 404/403 是客户端 UI 映射，不代表动态路由已返回对应 HTTP 状态 |
| `template/pingfangvideo/html/vod/downer_pwd.html` | 下载密码验证；由 `vod/down` 流程选择密码模板                                                    | React 页面          | `/vod/:vodId/download/unlock`，复用 `PasswordChallengePage`                             | `vod_id`、`vod_name`、目标 `sid`、`nid`；同源 CSRF POST 写入 `data-type=5` 对应 session                                                                                                                                                               | `R2`；下载密码 session 不能转作详情或播放授权                                                                               |
| `template/pingfangvideo/html/vod/play.html`       | 完整播放页；action `vod/play`，默认 `/index.php/vod/play/id/:vodId/sid/:sid/nid/:nid.html`      | React 页面          | `/watch/:vodId/:sourceId/:episodeId`，`WatchPage`                                       | `vod_id`、`vod_name`、当前组和集名称、全部可选组及集元数据、上一集和下一集标识；React 嵌入后端播放器承载面；用户日志 type 4、选集、上下集和自动下一集                                                                                                 | `R3`；新旧 URL 必须完整保留 `sid`、`nid`，最后一集不得循环到自身                                                            |
| `template/pingfangvideo/html/vod/player.html`     | 试看、收费或权限场景的 iframe 播放器模板；action `vod/player`，由 MacCMS 播放流程内部生成       | MacCMS 后端直通     | 同源内部播放器 URL，由 `WatchPage` iframe 或受授权桥接响应承载；不注册为 React 页面路由 | `$obj` 当前影片和播放组、`sid`、`nid`、当前集名称、`$player_data`、`$player_js`；MacCMS 负责付费/版权策略；试看模板按服务端授权分钟计时、阻止拖动绕过并到时替换播放器                                                                                 | `B1`、`R3`；响应必须 `private, no-store`，版权状态不能被试看/允许分支绕过，不能被 SPA 回退吞掉                              |
| `template/pingfangvideo/html/vod/player_pwd.html` | 播放密码验证；由播放流程选择密码模板                                                            | React 页面          | `/watch/:vodId/:sourceId/:episodeId/unlock`，复用 `PasswordChallengePage`               | `vod_id`、`vod_name`、`sid`、`nid`；同源 CSRF POST 写入 `data-type=4` 对应 session；原 iframe 密码模板也使用官方 `MAC.Pwd.Check`                                                                                                                      | `R2`、`R3`；播放密码 session 不能转作详情或下载授权                                                                         |
| `template/pingfangvideo/html/vod/plot.html`       | 单个视频的分集剧情；action `vod/plot`，默认 `/index.php/vod/plot/id/:vodId.html`                | React 页面          | `/vod/:vodId/plot`，`PlotPage`                                                          | `vod_id`、`vod_name`、`vod_blurb`、剧情条目 `name`、`detail`；返回详情；API 404 显示 `MissingContentPage`，403 复用 `detail` 权限状态                                                                                                                 | `R1`；HTML 内容先在后端清洗，空剧情有恢复入口；当前 404/403 是客户端 UI 映射，不代表动态路由已返回对应 HTTP 状态            |
| `template/pingfangvideo/html/vod/rss.html`        | 当前视频分类 RSS；action `vod/rss`                                                              | MacCMS 后端直通候选 | 保留原 MacCMS RSS URL；无 React 路由                                                    | `$obj.type_name`、`type_des`、站点信息；最新 50 条的 `vod_name`、详情链接、简介、更新时间                                                                                                                                                             | `F1`；如果退场则所有别名执行 `X1`                                                                                           |
| `template/pingfangvideo/html/vod/search.html`     | 视频搜索、频道筛选和分页；action `vod/search`，默认 `/index.php/vod/search/wd/:wd.html`         | React 页面          | `/search?wd=:wd&type=:typeId&page=:page`，`VodSearchPage`                               | `wd`、`type`、`page`；父子分类；结果 `vod_id`、名称、海报、演员、简介；分页；空结果                                                                                                                                                                   | `R1`；关键词正确编码，频道和分页参数在旧 URL `301` 后不丢失，空关键词在客户端和服务端均拒绝                                 |
| `template/pingfangvideo/html/vod/show.html`       | 全站影片库筛选；action `vod/show`，默认 `/index.php/vod/show.html`                              | React 页面          | `/videos`，`VideoLibraryPage`                                                           | 固定首页五个业务频道及子类；真实父子分类上下文；`area`、`year`、`lang`、`class`、`letter`、排序和 24 条服务端分页；地区/年份/语言/剧情 facets 均由 `pingfangapi/content` 当前范围动态生成                                                             | `R1`；筛选组合、重置、分页、非法排序降级和动态筛选空态/错误回退                                                             |
| `template/pingfangvideo/html/vod/type.html`       | 单个视频分类及筛选；action `vod/type`，默认 `/index.php/vod/type/id/:typeId.html`               | React 页面          | `/category/:typeId`，复用 `VideoLibraryPage`                                            | `type_id`、`type_name`、`type_des`、父分类和扩展 `area`、`year`、`lang`；其余筛选、排序、分页和卡片 DTO 与影片库相同                                                                                                                                  | `R1`；不存在或停用分类返回 React 404，旧分类参数完整保留                                                                    |

### 标签和自定义页面

| 模板                                                | 原职责 / 旧路由                                                                      | 最终归属   | 新路由或承载组件                             | 关键数据 / 交互                                                                                                         | 验收方式                                                                     |
| --------------------------------------------------- | ------------------------------------------------------------------------------------ | ---------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `template/pingfangvideo/html/label/categories.html` | 视频分类入口；action `label/categories`，默认 `/index.php/label/categories.html`     | React 页面 | `/categories`，`CategoriesPage`              | 父级视频分类 `type_id`、`type_name`；最新、最热、评分快捷排序                                                           | `R1`；顶部“视频”和旧地址均进入独立分类页，旧地址单跳 `301`                   |
| `template/pingfangvideo/html/label/comics.html`     | 仅显示“漫画模块暂未启用”的兜底页；action `label/comics`                              | `410` 退场 | 无                                           | 无业务数据或写操作，仅跳首页和影片库                                                                                    | `X1`                                                                         |
| `template/pingfangvideo/html/label/history.html`    | 匿名浏览器本地观看记录；action `label/history`，默认 `/index.php/label/history.html` | React 页面 | `/history`，`LocalHistoryPage`               | `localStorage` 的 `pingfang_history` 或 `mac_history`；日期、时间、名称、继续播放 URL、海报、进度；不与登录账号记录混淆 | `R1`；无效 JSON、危险 URL、去重和空记录；登录账号记录仍走 `/account/history` |
| `template/pingfangvideo/html/label/hot.html`        | 本年度热播榜；action `label/hot`，默认 `/index.php/label/hot.html`                   | React 页面 | `/rankings/yearly`，`YearlyRankingPage`      | 当前年份；固定视频范围；按 `hits` 倒序；视频卡片 DTO 和分页                                                             | `R1`；年份边界、排序和分页对照 MacCMS 数据                                   |
| `template/pingfangvideo/html/label/videos.html`     | 全站最新影片；action `label/videos`，默认 `/index.php/label/videos.html`             | React 页面 | `/videos`，`VideoLibraryPage` 的默认最新视图 | 固定视频范围；按 `time` 倒序；视频卡片 DTO 和分页                                                                       | `R1`；与 `vod/show` 合并时每个旧地址分别单跳 `301`                           |

### 用户和设备

| 模板                                                    | 原职责 / 旧路由                                                                                         | 最终归属       | 新路由或承载组件                      | 关键数据 / 交互                                                                                                                                                                                       | 验收方式                                                                                                                |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `template/pingfangvideo/html/user/favs.html`            | 登录用户收藏列表；action `user/favs`，默认 `/index.php/user/favs.html`                                  | React 页面     | `/account/favorites`，`FavoritesPage` | 私有 DTO 的 `recordIds` 保留原生 `ulog_id`；视频 ID、名称、海报、备注和创建时间；勾选、单删、批删和清空；`favorites.delete` 仅在当前用户、`ulog_mid=1`、`ulog_type=2` 下按精确 `ulog_id` 删除         | `R2`；未登录跳 `/login`；确认弹窗、空/歧义选择和重复删除；选中影片不能扩展删除同一 `ulog_rid` 的隐藏记录                |
| `template/pingfangvideo/html/user/findpass.html`        | 找回密码；action `user/findpass`，默认 `/index.php/user/findpass.html`                                  | `410` 退场     | 无                                    | 账号找回明确排除出新前台；不保留 `/forgot-password` 页面，也不把旧地址重定向到登录页                                                                                                                  | `X1`；`/forgot-password` 与 `/index.php/user/findpass.html` 的 GET/HEAD 均返回精确 `410 Gone`                           |
| `template/pingfangvideo/html/user/foot.html`            | 用户页面底部 include，仅转引 `public/foot`；无独立路由                                                  | React 共享组件 | `AppShell`                            | 无独立数据或交互                                                                                                                                                                                      | `C1`；所有账号路由只保留一个应用骨架                                                                                    |
| `template/pingfangvideo/html/user/head.html`            | 用户页面头部 include，仅传用户中心标题给 `public/head`；无独立路由                                      | React 共享组件 | `AccountLayout` 和 `AppShell`         | 当前用户和站点公共数据                                                                                                                                                                                | `C1`；账号页面鉴权、标题和导航一致                                                                                      |
| `template/pingfangvideo/html/user/include.html`         | 用户模块公共资源 include，仅转引 `public/include`；无独立路由                                           | React 共享组件 | React 入口、全局样式和 provider       | 无独立数据；原 jQuery、`home.js` 和全局 `maccms` 由类型化 API 取代                                                                                                                                    | `C1`；React 账号页面不重复加载旧脚本                                                                                    |
| `template/pingfangvideo/html/user/index.html`           | 用户中心首页；action `user/index`，默认 `/index.php/user/index.html`                                    | React 页面     | `/account`，`AccountPage`             | 当前用户摘要；收藏、播放记录和设备入口                                                                                                                                                                | `R2`；匿名、会话过期和设备被撤销均回到 `/login` 且保留安全 return-to                                                    |
| `template/pingfangvideo/html/user/login.html`           | 登录页；action `user/login`，当前表单 POST `pingfangdevice/login`                                       | React 页面     | `/login`，`LoginPage`                 | 后台 `login_verify` 配置；`user_name`、`user_pwd`、`verify`、`openid`、`col`；验证码图片 `verify/index`；密码显隐、验证码刷新、防重复提交、成功回跳                                                   | `R2`；以 JSON `code=1` 判断成功；验证 Cookie 真正落盘、设备会话建立和错误提示，不以页面 200 代替登录成功                |
| `template/pingfangvideo/html/user/plays.html`           | 登录用户播放记录；action `user/plays`，默认 `/index.php/user/plays.html`                                | React 页面     | `/account/history`，`HistoryPage`     | DTO 按影片折叠展示最新进度，但 `recordIds` 保留该展示项代表的每个原生 `ulog_id`；`history.delete` 仅在当前用户、`ulog_mid=1`、`ulog_type=4` 下按所选 `ulog_id` 删除，清空才删除该用户全部 type 4 记录 | `R2`；折叠、单删、批删、清空、付费记录和跨用户隔离；不得按 `ulog_rid` 扩大删除，也不得用 `ulog_points=0` 排除受保护记录 |
| `template/pingfangvideo/html/user/reg.html`             | 用户注册；action `user/reg`，默认 `/index.php/user/reg.html`                                            | `410` 退场     | 无                                    | 新会员注册明确排除出新前台；登录页只服务既有会员，不展示注册入口，也不把旧注册地址重定向到登录页                                                                                                      | `X1`；`/register` 与 `/index.php/user/reg.html` 的 GET/HEAD 均返回精确 `410 Gone`                                       |
| `template/pingfangvideo/html/pingfangdevice/index.html` | 登录设备列表与撤销；插件 action `pingfangdevice/index`，默认形态 `/index.php/pingfangdevice/index.html` | React 页面     | `/account/devices`，`DevicesPage`     | `max_devices`；`session_id`、设备标签、`is_current`、登录和活跃时间、IP、UA、状态、撤销时间；POST `pingfangdevice/revoke` 的 `session_id`，同源凭据和 `X-Requested-With`                              | `R2`；不能撤销当前设备；确认后才禁用按钮；成功、业务失败、网络失败和设备上限淘汰均验证                                  |

### 评论、留言和报错

| 模板                                             | 原职责 / 旧路由                                                                             | 最终归属       | 新路由或承载组件                                                  | 关键数据 / 交互                                                                                                                                                            | 验收方式                                                                                                |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------- | -------------- | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `template/pingfangvideo/html/comment/ajax.html`  | 评论 Ajax HTML 片段；由 `comment/ajax` 按当前 `mid`、内容 ID 和页码返回，不是完整页面       | React 共享组件 | `CommentsPage` 内部评论列表；数据改由同源 JSON API 提供           | `commentEnabled=false` 时不发起列表请求；开启后只返回已审核的 `comment_name`、`comment_time`、已清洗 `comment_content`，不再把服务器 HTML 注入 React                       | `C1`、`R2`；JSON 契约成功、空态和恶意内容；关闭评论时无请求和无表单；旧 HTML Ajax 地址不得进入 SPA      |
| `template/pingfangvideo/html/comment/index.html` | 独立评论页；action `comment/index`，上下文携带 `mid` 和内容 ID                              | React 页面     | `/comments/:mid/:contentId`，`CommentsPage`；详情页可复用同一组件 | 会话下发 `commentEnabled`、`commentLogin`、`commentCaptcha`、`commentAudit`；关闭时展示固定关闭状态，PHP 再以 403 阻断；提交返回 `pending` 或 `published` 并显示对应文案   | `R2`；开关、匿名/登录策略、验证码、审核态、XSS 清洗、频率限制和评论内容绑定；待审核评论不得伪装成已发布 |
| `template/pingfangvideo/html/gbook/index.html`   | 留言反馈；action `gbook/index`，默认 `/index.php/gbook/index.html` 或 rewrite `/gbook.html` | React 页面     | `/feedback`，`FeedbackPage`                                       | 会话下发 `feedbackEnabled`、`feedbackLogin`、`feedbackCaptcha`、`feedbackAudit`；关闭时不渲染表单，PHP 再以 403 阻断；保存结果明确返回 `pending`/`published`               | `R2`；字段长度、登录策略、验证码、频率限制和审核状态；待审核留言不得显示为已发布                        |
| `template/pingfangvideo/html/book/index.html`    | `gbook/index` 的兼容别名页面，内容和提交目标相同；action `book/index`                       | React 页面     | `/feedback`，复用 `FeedbackPage`                                  | 与 gbook 相同，共享 `enabled`、登录、验证码和 `audit` 语义；无独立业务数据                                                                                                 | `R2`；旧 `book/index` 和 `gbook/index` 分别单跳 `301` 到同一新路由；关闭与审核态必须一致                |
| `template/pingfangvideo/html/book/report.html`   | 片源报错；action `book/report`，默认 `/index.php/book/report.html`                          | React 页面     | `/report`，`ReportPage`                                           | 旧地址可只提交原因和描述；详情/播放页进入时可附加并验证 `vodId`、`sid`、`nid`；复用 Gbook 的 `feedbackEnabled`、登录、验证码和 `feedbackAudit`，返回 `pending`/`published` | `R2`；旧地址直达、带影片/剧集上下文、开关、验证码、审核态、重复提交和后台可追踪性                       |

### 公共模板和共享片段

| 模板                                                        | 原职责 / 旧路由                                                                    | 最终归属       | 新路由或承载组件                                          | 关键数据 / 交互                                                                                                                                       | 验收方式                                                                                             |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `template/pingfangvideo/html/public/digg.html`              | 详情页顶踩 include；无独立路由                                                     | React 共享组件 | `RatingActions`                                           | `vod_id`、`vod_up`、`vod_down`、`mid`；原 `.mac_digg` 由 `home.js` 发起请求                                                                           | `C1`、`R2`；一次性状态、重复投票、匿名策略和并发计数                                                 |
| `template/pingfangvideo/html/public/foot.html`              | 关闭 `<main>`，提供计时钩子并加载 `app.js`；无独立路由                             | React 共享组件 | `AppShell` 和路由级 effect                                | 原 `.mac_timming`、`app.js` 初始化；迁移后按功能拆入 React hook，不加载旧 `app.js`                                                                    | `C1`；所有 React 路由无重复监听器，导航后计时和清理正确                                              |
| `template/pingfangvideo/html/public/head.html`              | 全站 HTML 头、导航、搜索、主题、用户菜单和移动抽屉；无独立路由                     | React 共享组件 | `AppShell`、`SiteHeader`、`MobileDrawer`、`ThemeSwitcher` | 站点名、当前用户、父级分类、搜索词；主题 localStorage、登录或退出、抽屉焦点、搜索非空校验                                                             | `C1`、`R2`；键盘焦点循环、Escape、路由高亮、退出 POST 和移动端抽屉                                   |
| `template/pingfangvideo/html/public/include.html`           | favicon、viewport、主题预加载、CSS、jQuery、全局 `maccms` 和 `home.js`；无独立路由 | React 共享组件 | React `index.html`、入口样式和 `AppProviders`             | 构建期品牌资源；运行期配置改由白名单 API；主题在首帧前恢复                                                                                            | `C1`；无闪烁主题；production 不加载 jQuery、`home.js` 或开发地址                                     |
| `template/pingfangvideo/html/public/jump.html`              | 控制器通用成功跳转模板；没有独立稳定 URL，由触发 action 提供 `$msg`、`$url`        | React 共享组件 | `/status`，`StatusPage`；写请求结果优先留在所属页面       | 公开路由固定显示“系统提示 / 当前操作已完成”，忽略查询参数中的 `title`、`message`；只接受站内允许列表中的 `to`，倒计时限制为 0–10 秒且可取消或立即跳转 | `C1`、`R2`；外部、协议相对、反斜杠和非允许路径回退到 `/`；公开 URL 不能伪造成功或系统消息            |
| `template/pingfangvideo/html/public/msg.html`               | 控制器通用系统提示模板；没有独立稳定 URL                                           | React 共享组件 | `StatusPage` 和各页面错误/权限边界                        | 直接访问 `/status` 只显示固定通用文案和安全恢复目标；真实业务错误、404、403 与超时由发起请求的页面边界根据 API 状态展示，不从公开查询参数读取消息     | `C1`；固定文案、防消息注入和安全回跳；动态内容 404/403 当前仅保证正确 UI，不声明真实服务端 HTTP 状态 |
| `template/pingfangvideo/html/public/paging.html`            | 共享分页 include；无独立路由                                                       | React 共享组件 | `Pagination`                                              | 当前页、总页数、上一页、下一页、URL 查询参数；页码跳转和上下界修正                                                                                    | `C1`；首尾页、单页隐藏、非法页码、浏览器前进后退和参数保留                                           |
| `template/pingfangvideo/html/public/score.html`             | 详情页综合评分展示 include；无独立路由                                             | React 共享组件 | `ScoreSummary`                                            | `vod_score`、`vod_score_num`                                                                                                                          | `C1`；零评分、缺失值和小数格式                                                                       |
| `template/pingfangvideo/html/public/star.html`              | 详情页星级展示 include；无独立路由                                                 | React 共享组件 | `StarRatingDisplay`                                       | `vod_score`；当前模板仅展示，不是输入控件                                                                                                             | `C1`；可访问名称与分值一致，不把展示误实现成未定义写接口                                             |
| `template/pingfangvideo/html/public/verify.html`            | 控制器通用验证码模板；没有独立稳定 URL，POST 回当前 action                         | React 共享组件 | `CaptchaChallenge`                                        | 验证码图片或挑战标识、`verify`；提交目标由调用页面明确传入                                                                                            | `C1`、`R2`；刷新、过期、错误、键盘操作和挑战不能跨 action 复用                                       |
| `template/pingfangvideo/html/public/vod_card.html`          | 视频网格共享卡片；无独立路由                                                       | React 共享组件 | `VodCard`                                                 | `vod_id`、`vod_name`、`vod_pic`、`vod_remarks`、`vod_score`、类型名称、年份；详情链接                                                                 | `C1`；图片错误回退、懒加载、可访问名称和干净详情 URL                                                 |
| `template/pingfangvideo/html/public/vod_filter_common.html` | 分类和影片库共享地区、年份、语言、字母、排序筛选；无独立路由                       | React 共享组件 | `VodFilterPanel`                                          | 当前类型和父类型扩展；全局扩展回退；`area`、`year`、`lang`、`letter`、`class`、`by`；排序白名单                                                       | `C1`；筛选组合、可用项动态收窄、选中项不被隐藏、重置和 URL 状态同步                                  |
| `template/pingfangvideo/html/public/vod_grid_results.html`  | 分类和影片库共享结果查询分支；无独立路由                                           | React 共享组件 | `VodGrid` 和 `useVodListQuery`                            | 24 条分页；类型范围和筛选参数；按 `hits`、`score` 或默认 `time`；空态                                                                                 | `C1`、`R1`；DTO、分页和三种排序对照，非法排序不能进入后端查询形状                                    |

### 纯兜底兼容模块

以下页面当前没有业务列表、内容操作或可用模块入口，仅输出统一“维护中”兜底。它们不值得为迁移而新建空 React 页面；完成生产数据和流量核对后返回 `410`。

| 模板                                              | 原职责 / 旧路由                                                                 | 最终归属            | 新路由或承载组件                     | 关键数据 / 交互                                               | 验收方式                                                                              |
| ------------------------------------------------- | ------------------------------------------------------------------------------- | ------------------- | ------------------------------------ | ------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `template/pingfangvideo/html/actor/detail.html`   | 演员详情兜底；action `actor/detail`，默认 `/index.php/actor/detail/id/:id.html` | `410` 退场          | 无                                   | 仅 `actor_name`、`actor_blurb` 和影片库或首页链接             | `X1`                                                                                  |
| `template/pingfangvideo/html/actor/index.html`    | 演员首页兜底；action `actor/index`，默认 `/index.php/actor/index.html`          | `410` 退场          | 无                                   | 无业务数据，仅影片库或首页链接                                | `X1`                                                                                  |
| `template/pingfangvideo/html/actor/search.html`   | 演员搜索兜底；action `actor/search`，默认 `/index.php/actor/search/wd/:wd.html` | `410` 退场          | 无                                   | 不执行演员查询，仅影片库或首页链接                            | `X1`；确认 rewrite `actorsearch/...` 别名也覆盖                                       |
| `template/pingfangvideo/html/actor/show.html`     | 演员筛选兜底；action `actor/show`                                               | `410` 退场          | 无                                   | 不执行筛选，仅影片库或首页链接                                | `X1`；确认 rewrite `actorshow/...` 别名也覆盖                                         |
| `template/pingfangvideo/html/actor/type.html`     | 演员分类兜底；action `actor/type`，默认 `/index.php/actor/type/id/:id.html`     | `410` 退场          | 无                                   | 仅 `type_name` 和恢复链接                                     | `X1`；确认 rewrite `actortype/...` 别名也覆盖                                         |
| `template/pingfangvideo/html/art/confirm.html`    | 文章权限确认兜底；由 `art/confirm` 流程选择                                     | `410` 退场          | 无                                   | 没有购买或确认写操作，仅恢复链接                              | `X1`                                                                                  |
| `template/pingfangvideo/html/art/detail.html`     | 文章详情兜底；action `art/detail`，默认 `/index.php/art/detail/id/:id.html`     | `410` 退场          | 无                                   | 仅 `art_name`、`art_blurb`，不输出文章正文                    | `X1`                                                                                  |
| `template/pingfangvideo/html/art/detail_pwd.html` | 文章密码页；由 `art/detail` 流程选择                                            | `410` 退场          | 无                                   | 当前有 `pwd`、`verify` 表单，但目标文章详情本身无业务内容     | `X1`；确认后台没有受密码保护的有效文章                                                |
| `template/pingfangvideo/html/art/index.html`      | 文章首页兜底；action `art/index`                                                | `410` 退场          | 无                                   | 无业务数据，仅恢复链接                                        | `X1`                                                                                  |
| `template/pingfangvideo/html/art/search.html`     | 文章搜索兜底；action `art/search`                                               | `410` 退场          | 无                                   | 不执行文章查询                                                | `X1`                                                                                  |
| `template/pingfangvideo/html/art/show.html`       | 文章筛选兜底；action `art/show`                                                 | `410` 退场          | 无                                   | 不执行文章筛选                                                | `X1`                                                                                  |
| `template/pingfangvideo/html/art/type.html`       | 文章分类兜底；action `art/type`                                                 | `410` 退场          | 无                                   | 仅 `type_name`，不输出文章列表                                | `X1`                                                                                  |
| `template/pingfangvideo/html/plot/udetail.html`   | 独立剧情详情兜底；action `plot/udetail`                                         | `410` 退场          | 无                                   | 仅通用 `name`、`detail`；当前没有独立剧情导航                 | `X1`；视频内分集剧情仍由 `/vod/:vodId/plot` 保留                                      |
| `template/pingfangvideo/html/plot/uindex.html`    | 独立剧情索引兜底；action `plot/uindex`                                          | `410` 退场          | 无                                   | 无业务数据，仅恢复链接                                        | `X1`                                                                                  |
| `template/pingfangvideo/html/role/detail.html`    | 角色详情兜底；action `role/detail`                                              | `410` 退场          | 无                                   | 仅 `role_name`、`role_blurb`                                  | `X1`                                                                                  |
| `template/pingfangvideo/html/role/index.html`     | 角色首页兜底；action `role/index`                                               | `410` 退场          | 无                                   | 无业务数据                                                    | `X1`                                                                                  |
| `template/pingfangvideo/html/role/show.html`      | 角色筛选兜底；action `role/show`                                                | `410` 退场          | 无                                   | 不执行角色查询或筛选                                          | `X1`                                                                                  |
| `template/pingfangvideo/html/topic/detail.html`   | 专题详情兜底；action `topic/detail`，默认 `/index.php/topic/detail/id/:id.html` | `410` 退场          | 无                                   | 仅 `topic_name`、`topic_blurb`，不输出专题内容                | `X1`；确认 rewrite `topicdetail-:id` 别名也覆盖                                       |
| `template/pingfangvideo/html/topic/index.html`    | 专题首页兜底；action `topic/index`                                              | `410` 退场          | 无                                   | 无业务数据                                                    | `X1`；确认 rewrite `topic-:page` 和 `/topic` 别名也覆盖                               |
| `template/pingfangvideo/html/website/detail.html` | 游戏或网址详情兜底；action `website/detail`                                     | `410` 退场          | 无                                   | 仅 `website_name`、`website_blurb`                            | `X1`                                                                                  |
| `template/pingfangvideo/html/website/index.html`  | 游戏或网址首页兜底；action `website/index`                                      | `410` 退场          | 无                                   | 无业务数据                                                    | `X1`                                                                                  |
| `template/pingfangvideo/html/website/search.html` | 游戏或网址搜索兜底；action `website/search`                                     | `410` 退场          | 无                                   | 不执行查询                                                    | `X1`                                                                                  |
| `template/pingfangvideo/html/website/show.html`   | 游戏或网址筛选兜底；action `website/show`                                       | `410` 退场          | 无                                   | 不执行筛选                                                    | `X1`                                                                                  |
| `template/pingfangvideo/html/website/type.html`   | 游戏或网址分类兜底；action `website/type`                                       | `410` 退场          | 无                                   | 仅 `type_name`，不输出列表                                    | `X1`                                                                                  |
| `template/pingfangvideo/html/art/rss.html`        | 文章 RSS；action `art/rss`                                                      | MacCMS 后端直通候选 | 保留原 MacCMS RSS URL；无 React 路由 | 站点信息；最新 50 条 `art_name`、详情链接、正文摘要、更新时间 | `F1`；由于文章页面计划 `410`，只有同时保留可达文章消费者链路时才可直通，否则执行 `X1` |

### RSS、Sitemap 和其他非页面输出

| 模板                                          | 原职责 / 旧路由                              | 最终归属            | 新路由或承载组件                     | 关键数据 / 交互                                          | 验收方式                                                     |
| --------------------------------------------- | -------------------------------------------- | ------------------- | ------------------------------------ | -------------------------------------------------------- | ------------------------------------------------------------ |
| `template/pingfangvideo/html/map/baidu.html`  | 百度 Sitemap XML；action `map/baidu`         | `410` 退场          | 无                                   | 站点地址和最新 100 条视频链接；属于明确不实施的 SEO 输出 | `X1`；`Content-Type` 和 Nginx 规则不得把它回退到 React HTML  |
| `template/pingfangvideo/html/map/google.html` | Google Sitemap XML；action `map/google`      | `410` 退场          | 无                                   | 站点地址和最新 100 条视频链接；属于明确不实施的 SEO 输出 | `X1`；`Content-Type` 和 Nginx 规则不得把它回退到 React HTML  |
| `template/pingfangvideo/html/map/rss.html`    | 全站视频 RSS；action `map/rss`               | MacCMS 后端直通候选 | 保留原 MacCMS RSS URL；无 React 路由 | 站点信息；最新 50 条视频名称、详情链接、简介和更新时间   | `F1`；与 `rss/rss` 的消费者和别名分别核对，无消费者执行 `X1` |
| `template/pingfangvideo/html/rss/baidu.html`  | 百度 Sitemap 别名输出；action `rss/baidu`    | `410` 退场          | 无                                   | 最新 200 条视频链接；属于明确不实施的 SEO 输出           | `X1`；不得因 `.xml` 或 `.html` 别名漏过规则                  |
| `template/pingfangvideo/html/rss/google.html` | Google Sitemap 别名输出；action `rss/google` | `410` 退场          | 无                                   | 最新 200 条视频链接；属于明确不实施的 SEO 输出           | `X1`；不得因 `.xml` 或 `.html` 别名漏过规则                  |
| `template/pingfangvideo/html/rss/rss.html`    | 全站视频 RSS 别名；action `rss/rss`          | MacCMS 后端直通候选 | 保留原 MacCMS RSS URL；无 React 路由 | 站点信息；最新 50 条视频名称、详情链接、简介和更新时间   | `F1`；与 `map/rss` 分别核对调用方，无消费者执行 `X1`         |

## 可复用页面族

| 页面族             | 复用边界                                                                                                                       | 覆盖模板                                                                                                                                                                         |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 应用骨架           | `AppShell`、`SiteHeader`、主题、抽屉、搜索、当前用户和路由状态                                                                 | `public/head`、`public/include`、`public/foot`、`user/head`、`user/include`、`user/foot`                                                                                         |
| 视频目录           | 一个查询模型和一套 `VideoLibraryPage`、`VodFilterPanel`、`VodGrid`、`VodCard`，由路由决定全站、分类、搜索或榜单参数            | `vod/show`、`vod/type`、`vod/search`、`label/categories`、`label/hot`、`label/videos`、`public/paging`、`public/vod_card`、`public/vod_filter_common`、`public/vod_grid_results` |
| 视频详情和权限挑战 | `VodDetailPage` 加通用 `PasswordChallengePage`、`AccessConfirmPage`、`AvailabilityPage`；所有授权由后端决定                    | `vod/detail`、`vod/detail_pwd`、`vod/confirm`、`vod/copyright`、`public/digg`、`public/score`、`public/star`、`public/verify`                                                    |
| 播放和下载         | `PlayerPage`、`DownloadsPage` 共享线路与集数 DTO；`PlotPage` 复用详情权限；播放器本体和最终资源地址继续后端授权                | `vod/play`、`vod/player`、`vod/player_pwd`、`vod/down`、`vod/downer_pwd`、`vod/plot`                                                                                             |
| 账号记录           | 统一用户日志 DTO 和批量删除交互；type 2 区分收藏、type 4 区分播放记录，折叠展示仍保留原生 `ulog_id` 并按精确 ID 删除           | `user/index`、`user/favs`、`user/plays`、`pingfangdevice/index`                                                                                                                  |
| 身份流程           | 只保留既有会员登录的验证码、重复提交和安全回跳；公开 `StatusPage` 只显示固定文案                                               | `user/login`、`public/jump`、`public/msg`、`public/verify`                                                                                                                       |
| 互动反馈           | 统一读取 `enabled`、登录、验证码和 `audit` 配置，执行同源写请求并按 `pending`/`published` 显示真实状态；评论额外绑定内容上下文 | `comment/index`、`comment/ajax`、`gbook/index`、`book/index`、`book/report`                                                                                                      |
| 本地历史           | 只处理经过 URL 白名单和结构校验的浏览器本地记录，不与账号记录混用                                                              | `label/history`                                                                                                                                                                  |

## 最小完成顺序

1. 先完成应用骨架、路由错误边界、DTO 基础类型和真实首页同源端点，使当前首页不再依赖 fixture。
2. 完成视频目录族：`/videos`、`/category/:typeId`、`/search`、榜单、分页、筛选和空态；这是详情与播放的主要入口。
3. 完成视频详情及评分、顶踩、收藏、密码和权限确认 API；接口只返回播放组和集数元数据。
4. 完成既有会员登录、当前用户、收藏、账号播放记录和设备撤销，先证明同源 Cookie 与设备会话闭环；注册和找回密码页面按 GET/HEAD `410` 退场。
5. 完成 React 播放外壳和下载页，同时保留并加固 `vod/player` 后端直通；用真实线路验证权限不可绕过。
6. 完成评论、留言、报错和本地历史；用 JSON API 替换 `comment/ajax` 的 HTML 注入。
7. 当前不实施 SEO；RSS、Sitemap 与 31 个退场项只保留本地 Proxy 契约，不作为 staging Nginx 切流门槛。
8. 完成干净 URL 直达刷新、PHP/API 优先级、静态资源缓存、候选端口健康检查、版本化发布和回滚验证。

## 矩阵完成门槛

- 文件枚举脚本确认 79 个模板路径在本矩阵中各出现一次，没有遗漏或重复。
- 26 个 React 页面均有路由、DTO、成功、空、无权限、404 和后端失败测试；涉及写操作的页面另有安全与会话测试。
- 17 个共享组件不再依赖 jQuery、`home.js` 或全局 `maccms` 对象；React 页面不加载旧 `app.js`。
- `vod/player` 和保留的 RSS 端点不经过 SPA，且 Nginx 顺序不会让 `/api.php/**`、后台、上传和插件端点落入 React。
- Next.js 直达的 31 个退场项返回 `410`；其中 `/register`、`/forgot-password` 及对应旧 GET 页面地址必须覆盖 GET/HEAD。staging 中仍由 MacCMS 接管的其他 `/index.php/**` 不纳入该断言。未匹配路由不得统一跳首页；动态内容不存在当前仅有客户端 404 UI，若上线门槛要求真实 HTTP 404，仍需单独完成服务端数据判定。
- 完成全仓门禁：`npm test`、`npm run lint`、`npm run lint:template`、`npm run verify:compat`、`npm run verify:preview`、React lint、类型检查、测试、Playwright E2E 和生产构建。
