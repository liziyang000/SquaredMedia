---
type: codex_conversation
project: "[[项目管理/项目/SquaredMedia]]"
thread_id: "019f6974-f4cc-7963-9fe8-1473d2cd1b84"
date: 2026-07-16
status: "已归档"
category: "插件"
branch: "master"
turns: 8
changed_files: 21
has_local_changes: true
source: Codex
synced_at: 2026-08-10
tags:
  - 项目管理/Codex
  - 项目管理/Codex/插件
---

# 分析 device 插件改进

> [!info] 来源与边界
> 本页来自 Codex 用户任务 `019f6974-f4cc-7963-9fe8-1473d2cd1b84`，以工作目录 `/Users/bytedance/Documents/SquaredMedia` 精确匹配。
> 归档保留用户请求、最终结果和文件变更事件；系统提示、内部推理、密钥、敏感地址与冗长工具输出不写入项目。
> 历史任务反映当时分支和代码状态，不自动代表当前实现；当前事实见 [[项目管理/Codex/代码逻辑|代码逻辑]]。

## 任务摘要

分析当前的device插件，有哪些可以改进的地方

## 对话记录

### 1. 2026-07-16 13:45

- **用户请求**：分析当前的device插件，有哪些可以改进的地方
- **用户请求**：仅思考当前仓库版本，昨天的squareddevice不需要考虑

- **结果**：## 结论 仅看当前 `master`（`f7226dd`）里的 `addons/pingfangdevice`：基础方案合理，随机 Token、哈希存储、用户归属校验和自动踢旧设备都已具备，但“最多 3 台设备”目前还不能作为可靠的安全边界。最优先应修复设备 Token 缺失时的放行逻辑。 ### 优先级改进项 | 优先级 | 问题 | 影响与建议 | |---|---|---| | P0 | 设备 Token 缺失时直接放行 | [DeviceSession.php:42](addons/pingfangdevice/service/DeviceSession.php:42) 在 `pfv_device_token` 为空时直接返回，而 [currentUser():189](addons/pingfangdevice/service/DeviceSession.php:189) 只检查 MacCMS 原生登录。删除设备 Cookie、绕过插件调用原生登录接口，都可以脱离设备数量限制。应改成：存在原生会员身份时必须同时存在有效设备会话，否则退出登录；上线时给旧会话设置一次迁移宽限期。 | | P0 | 设备会话没有服务端过期时间 | Cookie 只在登录时设置 30 天，但数据库查询只判断 `revoked_time=0`，不判断 `login_time`/`last_seen_time`。保存下来的原始 Token 可以长期重放。应增加绝对有效期和空闲有效期，并定期清理历史记录。 | | P1 | 登录存在“成功一半”的状态 | [Index.php:30](addons/pingfangdevice/controller/Index.php:30) 先调用 MacCMS 登录、设置原生 Cookie，再写设备表；设备表写入失败时，用户已经登录却没有设备会话，并会命中上述放行漏洞。建议捕获注册失败并回滚登录，随后把插入、数量限制置于事务内，成功提交后再写设备 Cookie。 | | P1 | 登录参数与 MacCMS 契约不完整 | [login.html:3](template/pingfangvideo/html/user/login.html:3) 只提交账号和密码，没有 `verify/openid/col`。MacCMS 官方 `User::login()` 会直接读取这些字段，启用登录验证码后当前表单必然失败。[官方 User 模型](https://github.com/magicblack/maccms10/blob/master/application/common/model/User.php#L637-L742)。应补默认字段和按配置展示验证码，并明确插件支持的最低 MacCMS 版本，因为它依赖 `return_meta`。…
- **轮次状态**：completed

### 2. 2026-07-16 13:58

- **用户请求**：完善功能

- **结果**：已完成当前仓库 `pingfangdevice` 的功能完善，未触碰 `squareddevice`。 主要改进： - [DeviceSession.php](addons/pingfangdevice/service/DeviceSession.php:18)：登录事务化、并发设备锁、失败回滚、服务端过期、活动更新节流、Cookie 配置生效。 - 原生注册/OAuth 首次登录自动纳管；已纳管会话删除 Token 后无法绕过设备限制。 - 设备上限支持 1–20 台，会话有效期支持 1–365 天。 - [控制器](addons/pingfangdevice/bridge/Pingfangdevice.php:23)：登录、退出和踢设备收紧为同源 Ajax POST；会话创建失败会撤销原生登录。 - [登录模板](template/pingfangvideo/html/user/login.html:15)：补齐验证码及 MacCMS 登录参数，符合官方[模板变量](https://www.maccms.la/theme/tags-global)和[当前登录接口](https://github.com/magicblack/maccms10/blob/master/application/common/model/User.php)。 - 设备 UA、IP 等输出转义，不再向模板暴露 Token 哈希。 - [VodFilterOptions.php](addons/pingfangdevice/service/VodFilterOptions.php:57)：候选筛选由 N+1 查询改为单次分组查询。 - [install.sql](addons/pingfangdevice/install.sql:1)：提供已有安装的增量数据库迁移。 - [deploy-theme.sh](scripts/deploy-theme.sh:82)：部署时验证 PHP、Hook 和数据库字段。 验证全部通过： - `npm test` - `npm run lint:template` - `npm run verify:compat` - `npm run verify:preview` - `npm run package` - `npm run verify:release` - PHP 语法检查及 Shell 语法检查 改动位于 `codex/feat_improve_device_sessions`，尚未提交或部署。首次部署会自动增加 `login_check_hash` 字段。
- **轮次状态**：completed

### 3. 2026-07-16 14:24

- **用户请求**：提交到主干，并推送到远程，部署到服务器

- **结果**：已完成提交、推送和生产部署。 - 提交：[`731e16e`](https://github.com/liziyang000/SquaredMedia/commit/731e16efd3754e5b108f7ed8736369812daa44ad) `feat: improve device session management` - 主干：本地 `master` 与 `origin/master` 完全同步，工作区干净 - 部署：`/www/wwwroot/squaredMedia` - 本次未包含任何 `squareddevice` 改动 - 测试、模板检查、兼容性检查、发布包验证全部通过 - 插件安装、Hook、数据库 `login_check_hash` 字段验证成功 - 关键远端文件与本地哈希完全一致 - [生产首页](https://www.ping2video.xyz/) 和[登录页](https://www.ping2video.xyz/index.php/user/login.html)均返回 200 - 游客访问设备管理页会正确跳转至登录页 - 线上资源版本：`08965df07873` 补充：`gh` CLI 的令牌已经失效，但系统 Git 凭据推送成功；仅在以后使用 `gh` 操作 PR/Issue 时需要重新登录。
- **轮次状态**：completed

### 4. 2026-07-16 14:57

- **用户请求**：整理本地分支，没用的删去

- **结果**：本地分支已安全整理。 已删除： - `codex/feat_improve_device_sessions` - `codex/fix_ping2_deploy_path` 已保留： - `master` - 当前 `codex/feat_automate_cleantalk_firewall_rules`：有未跟踪数据文件 - `fix/repair_remote_posters`：worktree 内有未提交修改 - `codex/douban-rating-integration`：存在未合并提交 - `codex/template_experience_polish`：存在未合并提交 没有失效的 worktree 可清理，也没有强制删除任何未合并工作。
- **轮次状态**：completed

### 5. 2026-07-16 14:59

- **用户请求**：当前分支修改删了吧，切换到fix/repair_remote_posters

- **结果**：已完成： - 删除 `cleantalk-public-latest-60-2026-07-16.json` - 删除已放弃分支 `codex/feat_automate_cleantalk_firewall_rules` - 仓库根目录已切换到 `fix/repair_remote_posters` - 原 repair worktree 已移除，其未提交修改完整迁移到当前目录： - `package.json` - `docs/maccms-vod-poster-repair.md` - `scripts/repair-vod-posters.php` - `tests/poster-repair.test.php` - 原有两条历史 stash 保持不变
- **轮次状态**：completed

### 6. 2026-07-16 15:11

- **用户请求**：当前分支的作用是什么，在修改什么问题

- **结果**：当前分支 `fix/repair_remote_posters` 用于修复 MacCMS 数据库中的失效视频海报，不是修改前台样式。 它处理的问题是：`vod_pic` 为空，或者仍是 `upload/vod/...` 这类本地相对路径，导致服务器上图片缺失、页面海报无法显示。脚本会： 1. 优先根据 `vod_douban_id` 从豆瓣获取海报，并校验返回 ID。 2. 其余影片查询后台配置的采集源。 3. 只有片名规范化后完全一致、年份不冲突时才匹配；同名结果有歧义就跳过。 4. 默认只生成 JSONL 预演报告，不修改数据库。 5. 正式应用前创建 `vod_pic_repair_backup` 备份表，支持回滚。 6. 更新时再次确认原记录仍是异常值，避免覆盖并发人工修改。 主要修改： - [repair-vod-posters.php](scripts/repair-vod-posters.php:486)：核心扫描、匹配、备份和更新工具 - [maccms-vod-poster-repair.md](docs/maccms-vod-poster-repair.md:1)：预演、应用和回滚说明 - [poster-repair.test.php](tests/poster-repair.test.php:1)：片名、年份、豆瓣 ID、歧义匹配等测试 - [package.json](package.json:7)：把海报修复测试加入 `npm test` 当前状态： - 所有修改都还未提交。 - 分支本身没有独立提交，并且落后 `master` 3 个提交。 - PHP 语法检查、海报修复测试和 `npm test` 均已通过。 - 尚未在服务器数据库执行，因此目前还不知道实际能修复多少条记录。 - 只处理空值和非 HTTP 地址，不会检测已经是 HTTP 地址但实际返回 404 的海报。
- **轮次状态**：completed

### 7. 2026-07-16 15:16

- **用户请求**：当前分支修改是否还有改进空间，为什么会出现vod_pic是upload/vod/... 这类本地相对路径的情况

- **结果**：有改进空间，而且目前不建议直接执行 `--apply`。整体方向是对的：严格匹配、默认预演、备份、歧义跳过都很好；但目标识别存在一个关键误判。 ### 为什么会出现 `upload/vod/...` 这不是异常格式，而是 MacCMS 的正常设计： 1. 采集源最初提供 HTTP 海报地址。 2. 开启“图片同步”后，MacCMS 会下载图片。 3. 本地模式把文件保存到 `ROOT_PATH/upload/vod/日期目录/文件名`。 4. 数据库只记录 `upload/vod/...` 相对路径。[MacCMS Collect.php](https://github.com/magicblack/maccms10/blob/b301dd60fd6ff69c8c8fff782e0c57af92818551/application/common/model/Collect.php#L583-L613)、[Image.php](https://github.com/magicblack/maccms10/blob/b301dd60fd6ff69c8c8fff782e0c57af92818551/application/common/model/Image.php#L7-L90) 后台手工上传海报也会生成同类相对路径。[Upload.php](https://github.com/magicblack/maccms10/blob/b301dd60fd6ff69c8c8fff782e0c57af92818551/application/common/model/Upload.php#L76-L82) 渲染时，`mac_url_img()` 会给相对路径补上站点路径，所以只要文件存在，这种记录完全有效。[MacCMS common.php](https://github.com/magicblack/maccms10/blob/b301dd60fd6ff69c8c8fff782e0c57af92818551/application/common.php#L2381-L2405) ### 当前服务器的实际情况 我刚刚只读检查了生产库和文件系统： | 类型 | 数量 | |---|---:| | 视频总数 | 223,234 | | HTTP/HTTPS 海报 | 222,007 | | `upload/vod/...` | 1,217 | | 相对路径对应文件存在 | 2 | | 相对路径对应文件丢失 | 1,215 | | 空海报 | 10 | 服务器当前上传模式确实是 `local`。三个缺失样本在整个 `/www/wwwroot` 下也没有找到。 因此可以确认： - 相对路径最初是 MacCMS 图片同步产生的正常数据。 - 后来数据库记录保留了，但对应的 `…
- **轮次状态**：completed

### 8. 2026-07-16 15:36

- **用户请求**：更改代码

- **结果**：代码已完成改进，当前分支已快进同步到最新 `master`，修改尚未提交，也没有执行生产数据库修复。 主要变化： - [repair-vod-posters.php](scripts/repair-vod-posters.php:22) - 本地模式只修复实际缺失的相对路径文件。 - 远程上传模式保留相对存储键。 - 保留 HTTP/HTTPS、`//`、`mac:` 地址。 - [并发保护](scripts/repair-vod-posters.php:167) - 更新同时比较 `vod_id` 与原始 `vod_pic`，不会覆盖预演后的人工修改。 - 写入前再次检查本地文件是否已经恢复。 - [报告安全](scripts/repair-vod-posters.php:172) - 报告只能创建新文件，禁止误追加或覆盖旧报告。 - [备份逻辑](scripts/repair-vod-posters.php:470) - 只备份本轮已匹配记录。 - `vod_pic` 长度与 MacCMS 的 1024 字段保持一致。 - [操作文档](docs/maccms-vod-poster-repair.md:3) 已同步新行为。 - [测试](tests/poster-repair.test.php:15) 覆盖本地文件存在/缺失、远程上传模式、协议地址、报告保护和原值锁。 - [package.json](package.json:7) 同时保留 device 与 poster 测试。 验证全部通过： - `npm test` - `npm run lint:template` - `npm run verify:compat` - `npm run verify:preview` - PHP 语法检查 - `git diff --check` 当前未执行 `--apply`，生产数据库没有被修改。
- **轮次状态**：completed

## 修改记录

- 分支快照：`master`
- 本地变更文件数：21
- 修改类型：本地文件修改


识别到的提交：`731e16e`、`08965df07873`。

| 操作 | 文件 |
| --- | --- |
| 修改 | `package.json` |
| 新增 | `tests/device-controller.test.php` |
| 新增 | `tests/device-session.test.php` |
| 修改 | `addons/pingfangdevice/service/DeviceSession.php` |
| 修改 | `addons/pingfangdevice/bridge/Pingfangdevice.php` |
| 修改 | `addons/pingfangdevice/controller/Index.php` |
| 修改 | `tests/device-controller.test.php` |
| 修改 | `addons/pingfangdevice/config.php` |
| 修改 | `addons/pingfangdevice/view/index/index.html` |
| 修改 | `template/pingfangvideo/html/pingfangdevice/index.html` |
| 修改 | `template/pingfangvideo/html/user/login.html` |
| 修改 | `template/pingfangvideo/js/app.js` |
| 修改 | `addons/pingfangdevice/service/VodFilterOptions.php` |
| 修改 | `tests/template.test.mjs` |
| 修改 | `tests/device-session.test.php` |
| 修改 | `addons/pingfangdevice/install.sql` |
| 修改 | `scripts/verify-release.mjs` |
| 修改 | `README.md` |
| 修改 | `scripts/deploy-theme.sh` |
| 修改 | `scripts/lint-template.mjs` |
| 修改 | `tests/poster-repair.test.php` |
| 修改 | `scripts/repair-vod-posters.php` |
| 修改 | `docs/maccms-vod-poster-repair.md` |

## 关联

- [[项目管理/项目/SquaredMedia]]
- [[项目管理/Codex/对话索引|Codex 对话索引]]
- [[项目管理/Codex/修改记录|跨任务修改记录]]
- [[项目管理/Codex/代码逻辑|当前代码逻辑]]
