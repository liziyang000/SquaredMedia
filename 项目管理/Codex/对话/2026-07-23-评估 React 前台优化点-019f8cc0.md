---
type: codex_conversation
project: "[[项目管理/项目/SquaredMedia]]"
thread_id: "019f8cc0-af53-7863-922e-e70e07c312fb"
date: 2026-07-23
status: "已归档"
category: "React 与设计"
branch: "codex/feat_switch_template_to_react"
turns: 1
changed_files: 0
has_local_changes: false
source: Codex
synced_at: 2026-08-10
tags:
  - 项目管理/Codex
  - 项目管理/Codex/React-与设计
---

# 评估 React 前台优化点

> [!info] 来源与边界
> 本页来自 Codex 用户任务 `019f8cc0-af53-7863-922e-e70e07c312fb`，以工作目录 `/Users/bytedance/Documents/SquaredMedia` 精确匹配。
> 归档保留用户请求、最终结果和文件变更事件；系统提示、内部推理、密钥、敏感地址与冗长工具输出不写入项目。
> 历史任务反映当时分支和代码状态，不自动代表当前实现；当前事实见 [[项目管理/Codex/代码逻辑|代码逻辑]]。

## 任务摘要

此项目要做苹果CMS后台，react+Next.js作为前台页面，当前还有什么需要优化的点

## 对话记录

### 1. 2026-07-23 10:15

- **用户请求**：此项目要做苹果CMS后台，react+Next.js作为前台页面，当前还有什么需要优化的点
- **用户请求**：当前工作区修改要作为已经验收的能力，在此基础上继续分析

- **结果**：结论：当前工作区能力可作为已验收基线；不建议重写 MacCMS 边界或把播放器搬进 Next。接下来应优先补“真实环境闭环”和“正式前台切流能力”。 | 优先级 | 仍需优化 | 建议 | |---|---|---| | P0 | 真实 MacCMS 权限验收 | 本地 E2E 使用的是 `react-api.php` fixture，需在 staging 固化专用账号与用例：游客、普通会员、VIP/付费、试看到期、密码、版权限制、设备撤销、篡改 `sid/nid`。验证 API 不泄露播放源，播放器仍二次鉴权。当前播放权限代码已具备，缺的是实环境持续验收。 | | P0 | 正式切流清单 | 迁移矩阵中 31 个页面会 `410`，注册/找回、Sitemap 和部分 RSS 也明确退场。正式主站切换前，应以后台模块、实际 rewrite 和近 30 天访问日志确认没有真实用户或外部消费者；有流量的页面不能直接退场。 | | P1（若重视自然流量） | SSR、SEO 与公共内容缓存 | 首页、目录、详情目前主要在客户端 `useQuery` 后加载，顶层 metadata 也是固定值；构建虽显示静态页面，实际内容不是服务端首屏数据。公共内容可改为服务端读取并设置受控再验证；账号、历史、播放器继续保持客户端与 `no-store`。同时补详情页 metadata、canonical、结构化数据、robots/sitemap。 | | P1 | 可观测性 | 目前有健康检查、原子切换和回滚，但缺少面向用户的指标。建议增加 API 按 action 的 p95/5xx、播放器 403/失败率、MacCMS 适配异常、发布后合成探测，以及前端 LCP/INP/CLS 与 JS 错误采集；日志不记录 Cookie、Token、播放地址。 | | P2 | 性能预算，而非盲目压缩 | 已有响应式与无溢出测试，但没有性能预算。先在真实移动网络测首页/详情/播放页，再决定是否延迟加载 GSAP、拆分非首屏组件、优化海报尺寸与 CDN 缓存。当前 Nginx 已长期缓存 `/_next/static`，动态 HTML/API 仍应按公开/私有数据分层缓存。 | | P2 | 安全响应头与播放器兼容 | API 已有同源、CSRF 与限流。当前仓库中的 staging Nginx 扩展未声明 CSP、Permissions-Policy 等；正式加固前要先盘点同源 iframe 播放器、第三方解析和图片域名，避免一刀切 CSP 破坏播放。 | 现有基础是可靠的：当前工作区已通过单测、PHP API/控制器测试、lint、类型检查、5 个浏览器 E2E 和 production standalone 构建。最值得先做的是第 1 项真实权限矩阵；它能直接验证 React 前台没…
- **轮次状态**：completed

## 修改记录

- 分支快照：`codex/feat_switch_template_to_react`
- 本地变更文件数：0
- 修改类型：无本地文件修改


本任务没有 Codex 本地文件变更事件。分析结论、远端操作或数据库操作若存在，保留在上方对应轮次结果中，不由本页额外推断。

## 关联

- [[项目管理/项目/SquaredMedia]]
- [[项目管理/Codex/对话索引|Codex 对话索引]]
- [[项目管理/Codex/修改记录|跨任务修改记录]]
- [[项目管理/Codex/代码逻辑|当前代码逻辑]]
