---
type: codex_conversation
project: "[[项目管理/项目/SquaredMedia]]"
thread_id: "019fbc0d-331d-7631-9e1b-47fbbed783e3"
date: 2026-08-01
status: "已归档"
category: "游戏与互动"
branch: "codex/chore_ignore_local_artifacts"
turns: 6
changed_files: 8
has_local_changes: true
source: Codex
synced_at: 2026-08-10
tags:
  - 项目管理/Codex
  - 项目管理/Codex/游戏与互动
---

# 梳理插件模板及手动部署

> [!info] 来源与边界
> 本页来自 Codex 用户任务 `019fbc0d-331d-7631-9e1b-47fbbed783e3`，以工作目录 `/Users/bytedance/Documents/SquaredMedia` 精确匹配。
> 归档保留用户请求、最终结果和文件变更事件；系统提示、内部推理、密钥、敏感地址与冗长工具输出不写入项目。
> 历史任务反映当时分支和代码状态，不自动代表当前实现；当前事实见 [[项目管理/Codex/代码逻辑|代码逻辑]]。

## 任务摘要

当前项目都包括哪些插件和模版，手动部署需要怎么做

## 对话记录

### 1. 2026-08-01 14:40

- **用户请求**：当前项目都包括哪些插件和模版，手动部署需要怎么做

- **结果**：当前仓库实际包含 1 套 MacCMS 模板、2 个插件，另外还有 2 个独立运行/交付单元。 ### 当前清单 | 类型 | 名称 | 用途 | 自动部署 | |---|---|---|---| | 模板 | `template/pingfangvideo`（PingFang Video 1.0.0） | 首页、分类、搜索、详情、播放、用户中心、游戏等完整 MacCMS V10 主题 | 是 | | 插件 | `pingfangdevice` 1.0.0 | 登录设备管理、视频筛选、线路质量检测、联机游戏票据 | 是 | | 插件 | `videolint` 1.0.0 | 视频库缺失字段、封面、播放源、重复数据扫描和 CSV 导出 | 否，需单独安装 | | 独立播放器 | `maccms-player` | ArtPlayer + hls.js 性能版播放器 | 会打包，但不会自动安装 | | 联机服务 | `services/game-server` | 五子棋、你画我猜 WebSocket 服务 | 是 | 插件的具体边界见 [docs/addons.md](docs/addons.md:6)，打包产物与部署范围见 [docs/development-and-operations.md](docs/development-and-operations.md:75)。 历史 Douban 评分插件只有方案文档，当前仓库不存在 `addons/douban`，不算现有插件。 ### 推荐的手动部署方式 这里的“手动部署”建议理解为：由你在本机明确执行仓库部署脚本，而不是在宝塔里逐文件覆盖。当前生产目标已经写在 [scripts/deploy-ping2.env](scripts/deploy-ping2.env:1)。 ```bash cd /Users/bytedance/Documents/SquaredMedia git status --short npm ci source scripts/deploy-ping2.env npm run deploy ``` `npm run deploy` 会自动完成： 1. 执行测试、Lint、模板检查、兼容验证和预览验证。 2. 重建并验证四个发布包。 3. 备份并安装 `pingfangdevice`。 4. 复制兼容控制器、执行 `install.sql`、登记 `app_begin` Hook。 5. 备份并替换 `template/pingfangvideo`。 6. 清理 MacCMS 缓存并执行站点回环检查。 7. 部署 `/opt/pingfanggames`、systemd 和 Nginx `/game-socket` 配置。 8. 检查游戏服务 `/healthz`。 完整流程见 …
- **轮次状态**：completed

### 2. 2026-08-01 14:43

- **用户请求**：我是指我的手动覆盖文件要怎么操作

- **结果**：明白。按你当前工作区的改动，生产环境只需要手动覆盖 2 个主题文件，不需要动插件、播放器或游戏服务。 ### 本次覆盖文件 | 本地文件 | 服务器目标 | |---|---| | `dist/pingfangvideo/js/app.js` | `/www/wwwroot/squaredMedia/template/pingfangvideo/js/app.js` | | `dist/pingfangvideo/html/public/foot.html` | `/www/wwwroot/squaredMedia/template/pingfangvideo/html/public/foot.html` | 必须使用 `dist/pingfangvideo/` 里的文件，不要直接上传 `template/pingfangvideo/html/public/foot.html`。源码里还是版本占位符，打包后才会变成： ```html app.js?v=fb65bd2e6f6a ``` ### 宝塔操作顺序 1. 先备份线上这两个文件，或者直接压缩备份整个： ```text /www/wwwroot/squaredMedia/template/pingfangvideo ``` 2. 先覆盖： ```text template/pingfangvideo/js/app.js ``` 3. 再覆盖： ```text template/pingfangvideo/html/public/foot.html ``` 这样可以避免页面先引用新版本号，而新 JS 还没上传完成。 4. 在 MacCMS 后台执行“清理缓存”。如果从宝塔操作，则清空以下目录的内容，但保留目录本身： ```text /www/wwwroot/squaredMedia/runtime/cache /www/wwwroot/squaredMedia/runtime/temp /www/wwwroot/squaredMedia/application/admin/view/_cache /www/wwwroot/squaredMedia/application/index/view/_cache ``` 5. 浏览器使用 `Cmd+Shift+R` 强制刷新。 ### 覆盖后核对 当前正确的 `app.js` SHA-256 是： ```text fb65bd2e6f6adf256b14941cd38df1e76c1dcab4a61f513c8c5d44a962b27768 ``` 服务器可以执行： ```bash sha256sum /www/wwwroot/squaredMedia/template/pingfangvideo/js/app.js grep 'app.js?v=' /www/…
- **轮次状态**：completed

### 3. 2026-08-01 15:21

- **用户请求**：我想从0开始部署需要怎么做

- **结果**：按你当前的服务器恢复状态，已经完成“系统重装、SSH、宝塔安装”，接下来应从网站运行环境开始。不要先上传主题：主题依赖 MacCMS 核心、数据库和插件。当前恢复状态见 [服务器恢复记录](docs/server-reset-recovery-runbook.md:20)。 ## 正确部署顺序 ```text 宝塔运行环境 → 创建站点和数据库 → 安装/恢复 MacCMS → 恢复旧数据库 → 安装 pingfangdevice → 安装 pingfangvideo 主题 → 安装独立播放器 → 安装联机游戏服务 → 可选安装 videolint → 缓存、SSL、线上验收 ``` ### 1. 宝塔安装运行环境 在宝塔软件商店安装： - Nginx - MySQL - PHP 8.4 - Node.js 22 PHP 8.4 至少启用： ```text mysqli pdo_mysql gd zip opcache curl mbstring ``` 项目当前就是按 PHP 8.4、Node.js 22 验证的，见 [环境要求](docs/development-and-operations.md:7)。 3x-ui 与网站部署没有依赖关系，可以另行安装，但注意不要占用网站的 80/443 端口。 ### 2. 创建宝塔站点 在“网站 → 添加站点”中设置： ```text 域名：www.ping2video.xyz 根目录：/www/wwwroot/squaredMedia PHP：8.4 数据库：MySQL ``` 同时： - 添加实际需要的其他域名。 - 确认 DNS 已指向新服务器。 - 申请并启用 HTTPS 证书。 - 保存数据库名、用户名和密码到密码管理器，不要放进仓库。 ### 3. 安装或恢复 MacCMS 这个仓库不包含 MacCMS 核心，不能单独启动网站。 有旧备份时，优先恢复： 1. 原 MacCMS 网站文件。 2. 原生产数据库。 3. `upload/` 上传文件。 4. `application/database.php` 数据库配置。 5. 播放器、采集、支付、域名等后台配置。 没有备份时，只能安装全新的 MacCMS V10，然后重新配置和采集数据。官方宝塔安装流程见 [MacCMS Getting Started](https://www.maccms.la/guide/getting-started)。 完成后站点根目录至少应有： ```text application/ addons/ runtime/ static/ template/ upload/ vendor/ index.php ``` 先验证： - 首页能打开。 - 后台能登录。 - 数据库连接正常。 - 分类、详情等 MacCMS 默认页面不是…
- **轮次状态**：completed

### 4. 2026-08-01 20:21

- **用户请求**：帮我部署吧，现在应该没有ssh密钥，我要怎么做
- **用户请求**：端口也切换到了814
- **用户请求**：已加

- **结果**：部署已完成，生产站 `https://www.ping2video.xyz/` 可以正常访问。 已完成： - SSH 密钥已配置，服务器连接为 `[服务器地址]`。 - 部署配置已更新：[deploy-ping2.env](scripts/deploy-ping2.env:1)。 - Node.js 22、Nginx、PHP 8.2、MySQL 均正常。 - MacCMS 数据库正常：97 张表，约 23.4 万条视频数据。 - `pingfangvideo` 模板和 `pingfangdevice` 插件已部署。 - 游戏 WebSocket 服务已安装、启用，健康检查正常。 - 独立播放器文件与本地 SHA-256 完全一致，无需重复覆盖。 - 首页、播放页、登录页均通过浏览器、控制台和网络请求验收。 - 本地测试、Lint、兼容检查、预览检查及发布包验证全部通过。 同时修复了部署脚本中的 PHP OPcache 校验问题和首次冷缓存超时问题，涉及 [deploy-theme.sh](scripts/deploy-theme.sh:105) 和 [deploy-game-server.sh](scripts/deploy-game-server.sh:263)。 可回滚备份： - 模板：`pingfangvideo.backup.20260801203212` - 插件：`pingfangdevice.backup.20260801203211` - 游戏服务版本：`20260801T123415Z` 目前仅有两个非阻塞问题： - 首页有一张历史海报文件缺失，产生单个 404，属于上传文件/数据库恢复缺口。 - PHP CLI 存在重复加载 `mbstring` 的警告，不影响网站运行。 本地部署修复尚未提交或推送，我保留了原有未提交修改。
- **轮次状态**：completed

### 5. 2026-08-01 22:07

- **用户请求**：# Files mentioned by the user: ## codex-clipboard-dfcd05bd-2bf6-40f5-94ce-82434295602e.png: /var/folders/3l/d74zy1d176z7k8d9jlrh2_680000gn/T/codex-clipboard-dfcd05bd-2bf6-40f5-94ce-82434295602e.png ## My request for Codex:

- **结果**：已修复并重新部署。 根因是游戏服务只允许 `https://www.ping2video.xyz`，而截图中的页面来自裸域 `https://ping2video.xyz`，因此 WebSocket 握手被返回 `403`。 处理结果： - 两个域名均已加入精确 Origin 白名单。 - 已更新 [deploy-ping2.env](scripts/deploy-ping2.env:8) 和回归测试。 - 游戏服务已发布为 `20260801T141030Z`。 - 服务健康检查正常。 - 裸域和 `www` 域名均已通过真实票据握手，收到 `session.ready`。 - 未覆盖主题、数据库或播放器。 刷新游戏页面即可；控制台里已有的旧错误不会自动消失，可先清空控制台再观察。本地修改尚未提交或推送。
- **轮次状态**：completed

### 6. 2026-08-02 18:49

- **用户请求**：提交到主干并同步到远程

- **结果**：已提交到主干并同步远程。 - 分支：`master` - 提交：`23f1fd7 chore: record and harden production recovery` - 远程：`origin/master` - 本地与远程：ahead/behind `0/0` - 工作区：干净 - 完整测试、Lint、模板、兼容及预览检查全部通过 - 未创建 PR，未改写远程历史 `gh` 的旧令牌仍然失效，但直接 Git 推送凭据正常，不影响本次同步。
- **轮次状态**：completed

## 修改记录

- 分支快照：`codex/chore_ignore_local_artifacts`
- 本地变更文件数：8
- 修改类型：本地文件修改


识别到的提交：`23f1fd7`。

| 操作 | 文件 |
| --- | --- |
| 修改 | `README.md` |
| 修改 | `docs/development-and-operations.md` |
| 修改 | `scripts/deploy-game-server.sh` |
| 修改 | `scripts/deploy-ping2.env` |
| 修改 | `scripts/deploy-theme.sh` |
| 修改 | `tests/template.test.mjs` |
| 新增 | `/private/tmp/ping2-production-smoke.cjs` |
| 修改 | `/private/tmp/ping2-production-smoke.cjs` |
| 新增 | `/private/tmp/ping2-db-sample.php` |

## 关联

- [[项目管理/项目/SquaredMedia]]
- [[项目管理/Codex/对话索引|Codex 对话索引]]
- [[项目管理/Codex/修改记录|跨任务修改记录]]
- [[项目管理/Codex/代码逻辑|当前代码逻辑]]
