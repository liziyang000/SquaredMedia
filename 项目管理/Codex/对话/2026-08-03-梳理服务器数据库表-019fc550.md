---
type: codex_conversation
project: "[[项目管理/项目/SquaredMedia]]"
thread_id: "019fc550-bc18-79a1-9910-f0502f78d00c"
date: 2026-08-03
status: "已归档"
category: "数据与运维"
branch: "master"
turns: 6
changed_files: 0
has_local_changes: false
source: Codex
synced_at: 2026-08-10
tags:
  - 项目管理/Codex
  - 项目管理/Codex/数据与运维
---

# 梳理服务器数据库表

> [!info] 来源与边界
> 本页来自 Codex 用户任务 `019fc550-bc18-79a1-9910-f0502f78d00c`，以工作目录 `/Users/bytedance/Documents/SquaredMedia` 精确匹配。
> 归档保留用户请求、最终结果和文件变更事件；系统提示、内部推理、密钥、敏感地址与冗长工具输出不写入项目。
> 历史任务反映当时分支和代码状态，不自动代表当前实现；当前事实见 [[项目管理/Codex/代码逻辑|代码逻辑]]。

## 任务摘要

服务器数据库有哪些表，作用都是什么

## 对话记录

### 1. 2026-08-03 09:50

- **用户请求**：服务器数据库有哪些表，作用都是什么

- **结果**：已对生产服务器数据库做了实时只读查询，没有执行写入。 当前数据库为 `squaredmedia`，表前缀 `mac_`： - 共 **98 张表** - InnoDB 70 张，MyISAM 28 张 - 总占用约 **692 MiB** - 其中 **62 张表当前记录数估算为 0** - `mac_vod` 有 **234,519 条视频**，约 **647.6 MiB**，占全库约 93.6% - InnoDB 的行数来自 `information_schema`，属于估算值 ## 1. 内容与栏目 - `mac_vod`：视频主表。保存片名、分类、演员、年份、海报、豆瓣信息、播放线路、剧集地址、下载地址等核心数据。 - `mac_type`：统一分类树。视频、文章、漫画、网址等模块的分类都由它管理。 - `mac_actor`：演员、导演等人物资料库。 - `mac_role`：影视角色资料及角色与演员关系。 - `mac_art`：文章、资讯内容。 - `mac_manga`：漫画内容及章节信息。 - `mac_website`：网址导航内容。 - `mac_topic`：专题聚合页。 - `mac_live`：直播频道、播放地址等。 - `mac_live_category`：直播频道分类。 - `mac_annex`：上传附件记录。 - `mac_link`：友情链接。 目前实际有数据的主要表是：`mac_vod` 234,519、`mac_type` 87、`mac_live` 17、`mac_live_category` 4、`mac_annex` 1。 ## 2. 采集、搜索与去重 - `mac_collect`：资源站/API 采集配置，目前 12 条。 - `mac_cj_node`：旧式采集节点、页面规则和解析规则。 - `mac_cj_content`：旧式采集过程中暂存的内容。 - `mac_cj_history`：采集历史 MD5，用来防止重复采集。 - `mac_vod_search`：前台视频搜索结果缓存，目前约 1,545 条。它不是视频源数据。 - `mac_search_query_log`：用户搜索词日志，目前约 232 条。 - `mac_vod_repeat`：视频重复检测的辅助结果表，只保存重复项 ID 和名称，目前约 48,582 条。 - `mac_tmpart`：文章重复检测临时表。 - `mac_tmpmanga`：漫画重复检测临时表。 - `mac_tmpwebsite`：网址重复检测临时表。 - `mac_replace_log`：采集内容替换规则的执行记录。 需要注意：`mac_vod_search` 虽然是缓存表，但不建议直接全表清空；重新搜索会触发对 23 万视频的扫描。 ## 3. 管理…
- **轮次状态**：completed

### 2. 2026-08-03 09:59

- **用户请求**：有哪些是多余的吗

- **结果**：有，但真正“多余”的不多。对生产代码、插件目录、模型文件和计划任务做了只读引用核对后，结论如下。 ### 高置信度冗余表 - `mac_pingfang_video_lint_scan` - `mac_pingfang_video_lint_issue` 依据：`videolint` 插件已经不存在，生产代码和计划任务均无引用；最后一次扫描是 2026-06-30，21,807 条问题全部已标记解决。可先导出归档，再删除，约释放 **6.2 MiB**。 - `mac_danmuku_list` 旧版/拼写兼容弹幕表，当前为空且无任何引用；现在实际代码使用 `mac_danmaku`。可删除，空间收益约 0.05 MiB。 - `mac_seo_script` 当前为空，没有控制器、模型、计划任务或生产代码引用。可删除，但只节省约 0.03 MiB。 ### 可以归档后清理 - `mac_vod_area_lang_backup_20260706212430` - `mac_vod_area_lang_cleanup_backup_20260706212549` 两张都是 2026-07-06 地区/语言整理备份，无代码引用，合计约 **22 MiB**。如果对应数据整理已经验收，建议导出 SQL 归档后删除。 ### 暂时保留 - `mac_vod_merge_backup_20260802183952`：昨天的视频合并备份，仍处于合理回滚期。 - `mac_vod_pic_repair_backup`：海报修复的原值备份，本仓库修复脚本仍明确使用它。 - `mac_malicious_domain`：虽然生产代码和计划任务没有查到引用，但里面有 29 条启用的安全规则；在确认没有外部 WAF/脚本直连前不建议删除。 ### 不要因为空表而删除 分析、监控、外部资源、商城、签到、互动、AI 质量等表虽然很多当前为 0 行，但对应控制器、模型或工具仍存在。`mac_tmpart`、`mac_tmpmanga`、`mac_tmpwebsite`、`mac_vod_repeat` 也仍被 MacCMS 去重逻辑使用。`mac_vod_search` 是搜索缓存，只能按关键词定向处理，不建议删表或全量清空。 因此，当前合理清理范围是 **4 张确定废弃表，加 2 张已过验收后可归档的旧备份表**，预计最多释放约 **28.3 MiB**。本次没有删除或修改任何数据。
- **轮次状态**：completed

### 3. 2026-08-03 10:18

- **用户请求**：看看现在还需要删除哪些可以精简的

- **结果**：当前数据库已从 **98 张精简到 95 张**，占用从约 **692 MiB 降至 685.8 MiB**。之前的两个 videolint 表和旧弹幕表已经删除，释放约 6.2 MiB。 ### 现在建议继续清理 | 表 | 当前情况 | 建议 | |---|---:|---| | `mac_vod_area_lang_backup_20260706212430` | 约 11.52 MiB，2026-07-06 备份 | 数据整理已验收后，导出归档并删除 | | `mac_vod_area_lang_cleanup_backup_20260706212549` | 约 10.52 MiB，2026-07-06 备份 | 数据整理已验收后，导出归档并删除 | | `mac_seo_script` | 0 行、0.03 MiB，无代码/模型/计划任务引用 | 可以删除，空间收益很小 | 这 3 张表合计可释放约 **22.1 MiB**。这是当前最合理、边界最清晰的精简范围。 ### 暂时不要删 - `mac_vod_merge_backup_20260802183952`：8 月 2 日刚生成的视频合并备份，仍有回滚价值。 - `mac_vod_pic_repair_backup`：海报修复原值备份，修复脚本仍明确依赖它；应等海报修复完全验收后再归档。 - `mac_malicious_domain`：虽然没有查到生产代码或计划任务引用，但里面有 29 条启用的安全规则。只占 0.03 MiB，不值得冒险删除。 - `mac_vod_repeat`：约 7.03 MiB、8 月 2 日仍有更新。后台视频重复检测会主动查询、删除和重建它，不是废表。 - `mac_vod_search`：当前精确 1,365 条、约 3.03 MiB，数据时间覆盖 7 月 19 日至 8 月 2 日，属于活跃搜索缓存，不建议全量清空。 - `mac_douban_log`：702 条、约 1.58 MiB，最晚记录到 7 月 22 日；可以以后按保留期删旧日志，但目前收益很低。 - `mac_search_query_log`：239 条、约 0.06 MiB，不值得为了空间清理。 - `mac_admin_audit_log`：28 条，属于安全审计记录，应保留。 ### 不建议做的精简 数据库还有约 61 张被报告为 0 行的表，但合计只占约 **2.4 MiB**，而分析、监控、商城、互动、AI、任务等生产控制器和模型仍引用这些结构。批量删除会破坏功能，空间收益却很小。 也不建议对 `mac_vod` 执行 `OPTIMIZE TABLE`： - 当前约 647.6 MiB - 可回收碎片只有约 0.10 MiB - 碎片率约 0.016% - 它是 MyISAM，优…
- **轮次状态**：completed

### 4. 2026-08-03 10:31

- **用户请求**：现在怎么再优化一下呢

- **结果**：现在最值得做的不是继续删表，而是修复一个重复缓存代码问题、调整 MySQL 缓存参数，再为分类计数补一个复合索引。 ## 第一优先：修复 `mac_vod_repeat` 重复膨胀 生产代码的 `Vod::cacheRepeatWithName()` 有明显逻辑问题： - 每次只更新一个片名后，又无条件执行一次全库重复统计。 - 当前 `mac_vod_repeat` 已增长到 **53,668 行**。 - 实际不同片名只有 **17,935 个**。 - 多出了 **35,733 条重复缓存记录**。 - 慢日志显示全库重建平均约 **81 秒**，已经执行 4 次。 建议修改为： 1. 单片更新成功时，只删除并重新插入该片名的重复记录，然后直接返回。 2. 只有表不存在等异常时，才执行全量创建。 3. 编辑视频时，在执行 `UPDATE` 前读取旧片名；当前代码是在更新后读取，拿不到真正的旧片名。 4. 修复代码后，全量重建 `mac_vod_repeat` 一次。 验收标准： - `COUNT(*)` 与 `COUNT(DISTINCT name1)` 相等。 - 单个视频保存不再出现全库 `INSERT INTO mac_vod_repeat SELECT ... GROUP BY`。 - 重复检测页面结果正常。 - 预计该表可以从约 7 MiB 缩小到约 2–3 MiB。 这个修改位于生产 MacCMS 核心文件，不在当前仓库的主题/插件自动部署边界内，不能直接通过现有 `npm run deploy` 发布，需要先确定可维护的补丁来源和回滚方式。 ## 第二优先：调整 MySQL 参数 当前服务器只有约 1 GiB 内存，交换区 544 MiB 已用满。建议先做保守调整： ```ini [mysqld] table_open_cache = 256 max_connections = 50 key_buffer_size = 32M ``` 依据： - `table_open_cache=32`，运行约 37 小时已经发生 **56,807 次缓存溢出**，表缓存命中率只有约 54%。 - `max_connections=500`，实际峰值只有 7，配置过大，会放大每连接内存风险。 - `key_buffer_size=8M`，而 `mac_vod` 的 MyISAM 索引约 96 MiB；当前索引物理读取率约 1.25%，可先保守提高到 32 MiB。 - 临时表落盘率只有约 0.19%，所以暂时不需要增大 `tmp_table_size`。 - InnoDB 缓存命中率约 99.45%，暂时不用继续增加 `innodb_buffer_pool_size`。 建议先通过动态参数试运行，观察 24 小时后再持久化；不要直接重启 MySQL。…
- **轮次状态**：completed

### 5. 2026-08-03 10:58

- **用户请求**：修复重复缓存代码需要更改哪个文件

- **结果**：主要只需修改生产服务器上的： `/www/wwwroot/squaredMedia/application/common/model/Vod.php` 涉及两处： - 约 797–807 行：应在执行 `UPDATE` 前读取 `$old_name`，当前是在更新后读取。 - 约 964–978 行：修复 `cacheRepeatWithName()`，单片名更新成功后直接返回；只有表不存在等异常时才执行全量重复缓存重建。 `application/common/model/Collect.php` 的缓存失效逻辑可以后续单独优化，但不是本次修复必改文件。 注意：这个文件不在当前 SquaredMedia 仓库的主题/插件部署范围中，不能通过现有 `npm run deploy` 自动发布。
- **轮次状态**：completed

### 6. 2026-08-03 10:59

- **用户请求**：这个文件是否修改的影响是什么

- **结果**：影响属于“文件重要、修改逻辑范围较窄”。 `application/common/model/Vod.php` 是 MacCMS 视频核心模型，文件本身影响范围很大；但本次只修改重复缓存维护逻辑，正常情况下只影响 `mac_vod_repeat`，不会修改视频播放数据。 具体影响： - 后台新增、编辑、改名视频时，不再重复执行约 81 秒的全库重复统计。 - 正确更新旧片名和新片名对应的重复记录。 - 阻止 `mac_vod_repeat` 继续产生重复缓存行。 - 后台“重复数据”列表会更准确、更快。 - `mac_vod` 主表、播放地址、海报、豆瓣数据、前台搜索缓存和播放器不应受到直接影响。 主要风险： - 如果代码出现 PHP 语法错误，所有加载 `Vod` 模型的前后台视频功能都可能报错，包括列表、详情、采集和编辑，因此必须先执行 `php -l`。 - 如果增量维护遗漏某种删除/改名路径，重复列表可能暂时过期，但不会损坏视频主数据；重新执行一次重复缓存重建即可恢复。 - 文件属于 MacCMS 核心文件，未来升级 MacCMS 时可能被覆盖，需要记录补丁或纳入版本管理。 - 首次异常回退到全量重建时，仍可能执行一次约 80 秒的扫描。 建议验收： 1. 备份原始 `Vod.php`。 2. 修改后执行 `php -l application/common/model/Vod.php`。 3. 重建一次 `mac_vod_repeat`。 4. 确认总行数与不同 `name1` 数量一致。 5. 测试新增视频、编辑不改名、视频改名、后台重复列表。 6. 检查慢日志，确认单个视频保存不再触发全库 `INSERT ... SELECT ... GROUP BY`。 整体上，这是一个值得修的性能和正确性问题，但应按核心文件变更处理，不能直接在线随手覆盖。
- **轮次状态**：completed

## 修改记录

- 分支快照：`master`
- 本地变更文件数：0
- 修改类型：无本地文件修改


本任务没有 Codex 本地文件变更事件。分析结论、远端操作或数据库操作若存在，保留在上方对应轮次结果中，不由本页额外推断。

## 关联

- [[项目管理/项目/SquaredMedia]]
- [[项目管理/Codex/对话索引|Codex 对话索引]]
- [[项目管理/Codex/修改记录|跨任务修改记录]]
- [[项目管理/Codex/代码逻辑|当前代码逻辑]]
