---
type: codex_conversation
project: "[[项目管理/项目/SquaredMedia]]"
thread_id: "019fd0c3-b5ee-7f10-b19b-1c8471c993f6"
date: 2026-08-05
status: "已归档"
category: "插件"
branch: "master"
turns: 5
changed_files: 8
has_local_changes: true
source: Codex
synced_at: 2026-08-10
tags:
  - 项目管理/Codex
  - 项目管理/Codex/插件
---

# 查找不完整选项来源

> [!info] 来源与边界
> 本页来自 Codex 用户任务 `019fd0c3-b5ee-7f10-b19b-1c8471c993f6`，以工作目录 `/Users/bytedance/Documents/SquaredMedia` 精确匹配。
> 归档保留用户请求、最终结果和文件变更事件；系统提示、内部推理、密钥、敏感地址与冗长工具输出不写入项目。
> 历史任务反映当时分支和代码状态，不自动代表当前实现；当前事实见 [[项目管理/Codex/代码逻辑|代码逻辑]]。

## 任务摘要

这些选项是从哪里来的，为什么不完全

## 对话记录

### 1. 2026-08-05 15:12

- **用户请求**：# Files mentioned by the user: ## codex-clipboard-2d515134-129c-4b28-b1f3-cb4d988586df.png: /var/folders/3l/d74zy1d176z7k8d9jlrh2_680000gn/T/codex-clipboard-2d515134-129c-4b28-b1f3-cb4d988586df.png ## My request for Codex: 这些选项是从哪里来的，为什么不完全

- **结果**：结论：这些选项并不是从影片库自动完整枚举的，而是“MacCMS 配置白名单 + 数据可用性裁剪”。因此配置过期后，数据库里有新内容也不会自动补出来。 | 筛选项 | 来源 | | --- | --- | | 地区、年份、语言 | 当前栏目 `type_extend` → 父栏目 `type_extend` → 全局 `vod_extend_*`，见 [vod_filter_common.html](template/pingfangvideo/html/public/vod_filter_common.html:1) | | 字母 | 模板硬编码的 `A-Z、0~9`，见 [vod_filter_common.html](template/pingfangvideo/html/public/vod_filter_common.html:70) | | 可用性 | `pingfangdevice/filters` 查询 `mac_vod` 后隐藏无结果选项 | 我在 2026-08-05 15:16 只读检查了现网： - 电影、电视剧、动漫、综艺等顶级栏目的 `mac_type.type_extend` 都是空的，因此实际使用全局配置。 - 现网全局年份配置正好是 `2021,2020,…,2000`，所以截图里完全没有 `2022–2026`。 - 现网已经存在大量 2026 影片，但动态接口仍只返回 `2021–2000`。 根本原因在 [VodFilterOptions.php](addons/pingfangdevice/service/VodFilterOptions.php:42)：只要配置候选值不为空，就只验证这些候选值是否有数据，不会再从数据库补充候选值。前端 [app.js](template/pingfangvideo/js/app.js:1721) 也只会隐藏已有按钮，不会创建缺失按钮。 所以它的行为是： ```text 配置候选项 ∩ 当前分类有数据的值 ``` 而不是： ```text 数据库中全部实际值 ``` 截图和我当前检查的现网页面也不是完全同一快照：目前全局配置还有“新加坡、其它”，部分分类会因没有匹配影片而隐藏；接口另有 120 秒缓存。截图未包含页面 URL 和“类型”行，因此无法百分之百确认具体栏目。 最直接的临时修复是补齐全局 `vod_extend_year`；更可靠的修复是让配置只控制排序，同时合并数据库中合法的新年份、地区和语言。此次仅诊断，没有修改代码或线上配置。
- **轮次状态**：completed

### 2. 2026-08-05 15:25

- **用户请求**：怎么补全

- **结果**：最稳妥分成“立即补齐”和“永久修复”。 ### 立即补齐 在 MacCMS 全局视频扩展配置中，把年份改成： ```text 2026,2025,2024,2023,2022,2021,2020,2019,2018,2017,2016,2015,2014,2013,2012,2011,2010,2009,2008,2007,2006,2005,2004,2003,2002,2001,2000 ``` 保存后清理 MacCMS 配置/模板缓存；动态筛选接口还有最长 120 秒缓存。 这能立刻解决当前缺少 `2022–2026`，而且不需要修改影片数据库。 ### 永久修复 避免明年再次缺少 2027，需要同时修改两处： - [vod_filter_common.html](template/pingfangvideo/html/public/vod_filter_common.html:24)：年份候选自动生成“当前年份到 2000”。 - [VodFilterOptions.php](addons/pingfangdevice/service/VodFilterOptions.php:42)：接口使用相同的自动年份范围，再根据 `vod_status=1` 和当前分类过滤无数据年份。 - 同时提升 `CACHE_VERSION`，使部署后旧缓存立即失效。 地区和语言不建议直接把数据库原始值全部展示出来。应先统一诸如“大陆 / 内地 / 中国大陆”“台湾 / 中国台湾”等名称，再补入全局白名单，否则页面会出现大量重复和脏选项。 完成标准是：`2022–2026` 中有影片的年份出现、无影片年份自动隐藏、组合筛选和分页仍正常，并通过仓库规定的五项验证。 如果你要我实施，建议回复“按永久方案本地修改”；我会修改代码和测试，但不会自行部署线上。
- **轮次状态**：completed

### 3. 2026-08-05 15:31

- **用户请求**：还有更早之前的啊
- **用户请求**：语言和地区也是有其他地区2

- **结果**：对，你说得对。我前面把 `2000` 当下限不准确。现网只读核查显示： - 启用影片最早到 `1902`，`1903`、`1911–1920` 也确实有数据。 - 合法四位年份已经超过 80 种；当前接口上限只有 80，见 [VodFilterOptions.php](addons/pingfangdevice/service/VodFilterOptions.php:311)，即使改成数据库动态查询，早期年份仍会被截断。 - 地区存在德国、意大利、菲律宾、澳大利亚、巴西等大量有效缺项。 - 语言存在泰语、西班牙语、意大利语、印地语、俄语、葡萄牙语等有效缺项。 但数据库也存在脏值： - 地区共有 816 种原始写法，包括“内地”“中内地地”“香港,大陆”等。 - 语言共有 856 种原始写法，包括“国语国语”“其他”“英语,法语”等。 - 年份也有 `20258`、`20247` 等异常值。 所以“补全”有两种含义： 1. 展示全部原始值：会把几百个脏值直接暴露给用户，不建议。 2. 展示全部规范化后的有效值：推荐。 正确方案应是： - 年份：从数据库获取 `1900–当前年份`之间实际存在的四位年份，接口年份上限提高到 200；页面默认显示近年，增加“更早年份”展开区并按年代分组。 - 地区：建立规范地区列表和别名映射，例如“内地 → 大陆”“其他 → 其它”；德国、意大利、澳大利亚等真实地区补入“更多地区”。 - 语言：建立规范语言列表和别名映射，例如“国语国语 → 国语”“其他 → 其它”；泰语、西班牙语、俄语等补入“更多语言”。 - 接口：配置只负责排序和名称规范，不再作为硬白名单；实际有数据的规范值都可以返回。 - 页面：需要让 [app.js](template/pingfangvideo/js/app.js:1721) 能增加接口返回的新按钮，而不只是隐藏已有按钮。 - 数据库清洗属于另一项生产写操作；当前是 MyISAM，必须先备份并安排维护窗口，不能与前端补全混在一起直接执行。 因此，单纯把全局配置字符串补长仍然不够。建议实施“规范化动态筛选”：先改主题和插件实现完整展示，数据库脏数据只做审计，不立即改写。
- **轮次状态**：completed

### 4. 2026-08-05 15:40

- **用户请求**：给个方案

- **结果**：# Plan 将固定白名单改成“规范化动态筛选”：年份覆盖数据库中实际存在的 `1900–当前年份`，地区和语言先合并同义词、排除脏值，再按当前分类和组合条件返回可用项。界面采用“常用项 + 更多”折叠展示，避免一次铺开上百个按钮。 ## Scope - In: 筛选接口、主题筛选组件、规范化字典、只读审计/预演脚本、测试、性能验证和受控发布方案。 - Out: 未经单独授权不修改生产 `mac_vod`、不部署线上、不展示 816 个地区和 856 个语言原始脏值、不修改 MacCMS 核心。 ## Action items [ ] 固化筛选契约：年份只接受 `1900–当前年份`的四位数并按降序排列；地区和语言使用规范名称；仅返回当前栏目、其他已选条件及 `vod_status=1` 下确实有影片的选项。 [ ] 建立地区和语言规范化字典，覆盖“内地 → 大陆”“其他 → 其它”“国语国语 → 国语”等确定性映射；保留德国、意大利、澳大利亚、泰语、西班牙语、俄语等真实缺项，对“中内地地”等无法安全判断的值只记录审计、不自动猜测。 [ ] 重构 [VodFilterOptions.php](addons/pingfangdevice/service/VodFilterOptions.php:42)，把栏目/全局配置从“硬白名单”改成“排序优先级”；年份候选自动覆盖当前年到 1900，年份返回上限提高到 200，并继续过滤无数据年份和异常值。 [ ] 为地区、语言聚合规范值和别名计数，确保选择规范项后返回的影片范围与接口计数一致；避免在每次页面请求中执行全表正则或大量模糊查询，保留版本化缓存并记录首次查询与缓存命中的耗时。 [ ] 更新 [vod_filter_common.html](template/pingfangvideo/html/public/vod_filter_common.html:1) 和 [app.js](template/pingfangvideo/js/app.js:1681)，让前端能够新增、排序接口返回的选项，而不再只是隐藏已有按钮；链接仍通过 MacCMS URL helper 模板生成并保留其他筛选参数。 [ ] 调整筛选交互：年份默认显示最近 15 年，其余按年代放入“更早年份”；地区和语言保留常用项，其余进入“更多地区/语言”；当前选中项始终可见，并补齐键盘操作、展开状态和移动端换行验证。 [ ] 新增专门的 `VodFilterOptions` 行为测试，并扩展 [template.test.mjs](tests/template.test.mjs:1064)，覆盖 1902/1999/2026、异常年份、别名聚合、多地区/多语言、组合筛选、80 项截断回归、动态新增链接和无 JavaScript 服务端兜底。 [ …
- **轮次状态**：completed

### 5. 2026-08-05 15:47

- **用户请求**：根据服务器实际存在的动态生成过滤条件

- **结果**：方案已在本地分支完成，未部署、未改生产数据库。 - 地区、语言、年份改为从服务器 `mac_vod` 的实际字段索引动态读取；后台 `vod_extend_*` 配置只控制常用项顺序，不再限制候选范围。 - 年份保留 1900～当前年的有效值。生产演算得到 118 个年份，包括 2026、1902、1903，以及 1911～1920。 - 自动清洗脏数据并合并别名，例如： - `内地 / 中内地地` → 大陆 - `USA` → 美国 - `国语国语` → 国语 - 排除“没广告”“中国动漫”“加拿大语”等错列值 - 别名通过逗号组合成可筛选参数，符合 [MacCMS 视频筛选参数规范](https://www.maccms.la/theme/theme-vod)。 - 前端会创建服务端配置中原本不存在的链接；选项过多时限制高度并滚动展示。 - 最终索引查询生产实测约 `0.004～0.013 秒`，避免了原方案全表聚合约 `73～100 秒`的问题。 主要文件： - [VodFilterOptions.php](.worktrees/codex/fix_dynamic_vod_filters/addons/pingfangdevice/service/VodFilterOptions.php) - [vod_filter_common.html](.worktrees/codex/fix_dynamic_vod_filters/template/pingfangvideo/html/public/vod_filter_common.html) - [app.js](.worktrees/codex/fix_dynamic_vod_filters/template/pingfangvideo/js/app.js) - [vod-filter-options.test.php](.worktrees/codex/fix_dynamic_vod_filters/tests/vod-filter-options.test.php) `npm test`、Lint、模板检查、兼容检查和预览验证均已通过。 当前方案生成的是“全站实际存储候选”，不会随着当前栏目逐级收缩。若以后要求严格限定为“当前栏目且仅已发布影片”，需要为 MyISAM 表增加复合索引或建立物化缓存，应另安排维护窗口。
- **轮次状态**：completed

## 修改记录

- 分支快照：`master`
- 本地变更文件数：8
- 修改类型：本地文件修改


| 操作 | 文件 |
| --- | --- |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/package.json` |
| 新增 | `.worktrees/codex/fix_dynamic_vod_filters/tests/vod-filter-options.test.php` |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/addons/pingfangdevice/service/VodFilterOptions.php` |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/tests/vod-filter-options.test.php` |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/tests/template.test.mjs` |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/template/pingfangvideo/html/public/vod_filter_common.html` |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/template/pingfangvideo/js/app.js` |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/template/pingfangvideo/css/style.css` |
| 新增 | `.worktrees/codex/fix_dynamic_vod_filters/addons/pingfangdevice/service/VodFilterOptions.php` |
| 修改 | `.worktrees/codex/fix_dynamic_vod_filters/docs/addons.md` |

## 关联

- [[项目管理/项目/SquaredMedia]]
- [[项目管理/Codex/对话索引|Codex 对话索引]]
- [[项目管理/Codex/修改记录|跨任务修改记录]]
- [[项目管理/Codex/代码逻辑|当前代码逻辑]]
